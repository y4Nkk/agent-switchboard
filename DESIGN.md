# Agent Switchboard 设计系统

**版本：** 0.1
**状态：** 实施前已批准的视觉基线
**方向：** `Frosted Relay` — 面向双客户端配置控制台的 Apple 式层级与克制的高斯模糊。

## 1. 产品与视觉主张

**主题：** 一个安全管理 `Codex` 与 `Claude Code` 提供商配置的本地 Windows 桌面应用。

**受众：** 一位需要了解当前启用内容、即将变更内容及撤销方式的技术型用户。

**唯一任务：** 让当前路由和切换后果能够立即被理解。

这不是营销页面，也不是通用 `SaaS` 仪表盘。主视觉对象不应是横幅图，而应是真实路由状态：两条客户端轨道、各自启用的提供商节点以及明确的安全切换操作。

## 2. 文字是信息，而非装饰

界面文字必须配得上所占空间。产品管理的是配置，因此空白区域往往比一叠用来强调区域重要性的标签更合适。

- 每个工作区域最多只有一个任务标题。仅当辅助行会改变用户下一步决策，或解释不明显的风险时才添加。
- 不得只为让面板显得经过设计而添加眉题、“功能”副标题、营销短语或配置主题填充文案。
- 徽标或标签只可说明真实的当前属性：启用状态、校验结果、警告、筛选条件或客户端身份。它不是视觉分隔符，也不能代替间距。
- 不得在标题下放置成组的状态类标签。唯一有意义的状态应紧邻它修饰的控件或值。
- 字段标签、校验信息和配置路径注释仍是必要的功能性文字。保持简短，并紧邻其说明对象。
- 采用删除测试：移除一处文字后，若设置、操作、状态、警告或决策并未变得更难理解，就删除它。

`Dual Relay` 与配置内容提供层级。其余视觉工作必须由字体、留白、细分隔线和材质深度完成。

## 3. 参考解读

`SpiralCoder` 提供了有价值的实现纪律：

- 有文档记录的设计系统；
- 一个令牌所有者，而非组件局部的视觉常量；
- 中文界面文字使用 `MiSans`，代码使用专用字体；
- 按层级使用模糊，而非滥用玻璃效果；
- 低阴影、高可读性的玻璃表面。

`Agent Switchboard` **不**继承 `SpiralCoder` 的蓝粉品牌渐变、面向儿童的图像、大型展示区或装饰性粒子效果。本产品处理配置和凭据，因此平静与可审计性比表现力强的颜色更重要。

### 界面来源

`CC Switch` 和 `Codex++` 是后端操作参考，而非视觉参考。`Frosted Relay` 的布局、令牌、组件、交互设计、图标与界面文案均由本文件为 `Agent Switchboard` 创建。不得迁移任一项目的页面、组件结构、资源或文字。

## 4. 视觉标识：`Dual Relay`

产品令人记忆的元素是双客户端路由本身。概览页顶部以两张并排的路由卡呈现两条客户端轨道的当前状态；路由卡仅在概览出现，不再常驻其他页面（用户于 2026-08-27 指令）。卡片视觉完整复刻自 `spiralcoder` AI 权益工作台的模型服务卡（用户于 2026-08-27 指令），是本系统获准的渐变表面之一（另一处为通用设置滑块的能量渐变，2026-08-28 用户指令）：

- **已启用卡**：蓝→浅蓝→暖橙流动渐变加底部深蓝罩（`--asb-route-active-gradient`），`MatrixStarlightCanvas` 白色星光点阵（canvas 渲染，顶部渐隐 + 左上内容安全区，色调令牌 `--asb-route-particle-*-rgb`），白色文字与白底蓝字加绿点的状态徽标，底部为磨砂半透明的三值条。整卡仅此处允许投影（`--asb-route-shadow`）。
- **未加载卡**：近实心磨砂表面加重度背景模糊，蓝紫星光点阵（`--asb-route-particle-idle-*-rgb`，深色模式换高亮蓝紫），中性描边与灰字徽标。
- `Codex` 卡以行动蓝整边描边标识，`Claude` 卡用紫色整边描边；渐变两卡共用，颜色仅表示身份，不形成第二套操作色。
- 徽标文字（已启用 / 未加载）加圆点表达状态，绝不能只靠颜色。
- 卡内只呈现真实路由状态：客户端名左侧的品牌标瓦片（与供应商页分段控件同源的官方图标，装在固定浅色圆角瓦片上，深色渐变面与深色主题下均保持可读）、生效供应商名、模型、服务地址、接入方式。禁止用量统计、迷你图表或任何虚构数据——本产品没有流量数据来源。
- 玻璃嵌套豁免：未加载卡的背景模糊与值条的内层轻磨砂是本控件允许的例外；其余界面仍禁止玻璃内放玻璃。
- 粒子由 `MatrixStarlightCanvas` 以 30fps 上限绘制；减少动态效果时绘制单帧静止画面，入场为一次 240ms 上浮，按卡片序号错开 60ms（2026-08-28 用户指令：粒子升级为 `spiralcoder` 最新星光矩阵实现）。
- 概览的「配置状态」模块与路由卡同构（2026-08-28 用户指令）：同一面板内每个客户端一张磨砂状态卡，头部为品牌标瓦片加客户端名与带圆点的状态胶囊（配置正常 / 语法错误 / 未找到配置文件 / 读取失败，颜色不作唯一表达；品牌标瓦片与路由卡同款，2026-08-28 用户指令），下方为带标签的字段行——配置文件、当前服务、匹配状态、上次切换、范围警告、写入锁；遗留锁的清理按钮位于所属卡内，不再使用整宽裸文本键值堆。
- 概览可在配置状态之后放置一个独立的「本机会话」只读面板（2026-08-31）：它以两条客户端账本行呈现已读取的会话数量、最近本地记录更新时间与扫描提示，并提供进入既有会话页的入口。读取必须由用户显式触发；该面板不属于 `Dual Relay` 路由卡，不显示会话标题、路径、对话内容、token、成本、额度或图表，也不捏造未读取的数据。

## 5. 令牌计划

运行时令牌值最终将集中在一份样式表中，拟定为 `src/styles/tokens.css`。组件只能消费语义令牌。

### 核心色板

| 令牌 | 名称 | 值 | 作用 |
| --- | --- | ---: | --- |
| `--asb-ice` | 极光冰白 | `#F6F7FB` | 浅色画布与高光 |
| `--asb-ink` | 板岩墨黑 | `#14161B` | 主文字与高对比度内容 |
| `--asb-current` | 深流蓝 | `#071725` | 深色画布与环境背景 |
| `--asb-action` | 行动蓝 | `#0A84FF` | 按钮、焦点与 `Codex` 身份 |
| `--asb-claude` | `Claude` 紫 | `#8B7CFF` | `Claude` 身份与非操作性标签 |
| `--asb-safe` | 安全绿 | `#30D158` | 已验证与已完成状态 |

警告与错误仅使用 Apple 式系统语义色。它们不得成为第二套品牌色板。

### 字体

| 角色 | 字体栈 | 用途 |
| --- | --- | --- |
| 展示文字 | `Segoe UI Variable Display`, `MiSans`, `system-ui` | 页面标题与当前提供商 |
| 界面文字 | `Segoe UI Variable`, `MiSans`, `system-ui` | 标签、表单与导航 |
| 代码 | `Maple Mono`, `Cascadia Mono`, `ui-monospace` | `TOML`、`JSON`、路径和配置差异 |

- 未作出单独的许可证决定前，不得打包 `SF Pro`。
- 标题使用 `600` 字重，字距紧凑但易读。
- 正文使用 `15–16px` 与 `400` 字重。
- 配置数据使用等宽数字与代码字体。
- 展示字体必须克制：工作界面中不得出现超大的营销式标题。

### 形状与间距

| 令牌 | 值 | 用途 |
| --- | ---: | --- |
| `--asb-radius-window` | `28px` | 主外壳与大型面板 |
| `--asb-radius-panel` | `22px` | 主面板 |
| `--asb-radius-control` | `14px` | 输入框与辅助控件 |
| `--asb-radius-action` | `12px` | 主操作和破坏性操作按钮 |
| `--asb-radius-check` | `5px` | 复选框等小型勾选控件 |
| `--asb-space-unit` | `4px` | 原子间距单位 |

结构间距使用 `8`、`12`、`16`、`24`、`32` 和 `48px`。操作控件不自动采用药丸形；完整药丸形仅保留给紧凑状态控件和启用的提供商节点。

## 6. 材质系统

高斯模糊仅在其后方存在真实层级时才用于建立层次。

| 材质 | 用途 | 表面与模糊 |
| --- | --- | --- |
| 环境背景 | 应用背景 | 柔和蓝色流场，仅背景使用 `72px` 模糊 |
| 玻璃轨道 | 侧边栏与顶部路由栏 | `70%` 透明度、`28px` 模糊、`160%` 饱和度 |
| 玻璃菜单 | 菜单与短暂弹出层 | `78%` 透明度、`20px` 模糊 |
| 玻璃面板 | 差异确认与模态流程 | `58–68%` 透明度、`40px` 模糊 |
| 实心内容 | 表单、表格、代码预览、差异主体 | `88–94%` 不透明度，无背景模糊 |

实现规则：

- 将环境背景渲染为真实的、带 `aria-hidden` 的 DOM 层。可使用少量低不透明度的大型径向光源和模糊；不得出现可见的伪元素装饰。
- 玻璃使用 `1px` 白色或中性色细分隔线，以及一道轻微的内嵌高光。
- 阴影仅用于模态框或抬升的菜单。卡片默认不得悬浮。
- 绝不能在玻璃内嵌套玻璃。玻璃面板中应包含实心内容组。
- `backdrop-filter` 不可用时，提供实心不透明后备样式。
- 尊重系统浅色和深色模式。浅色模式使用极光冰白；深色模式使用深流蓝，绝不使用纯黑。

滚动条（2026-08-28 用户指令，自有设计）：全局以「墨线滚动条」替换 WebView2 默认滚动条——`10px` 槽位、轨道与按钮全透明（无凹槽底色），滑钮经透明描边内缩为 `4px` 墨色药丸线并保持全槽拖拽命中区，悬停加深、拖拽最强，最短 `32px`；深浅模式各用一套基于正文墨色的 tint，绝不引入第二强调色。规则唯一所有者是 `base.css` 的 `::-webkit-scrollbar` 全局块，值由令牌（`--asb-scrollbar-*`、`--asb-radius-pill`）提供，覆盖工作区、配置预览与文本域等一切滚动表面。不得混用标准 `scrollbar-width` / `scrollbar-color`（Chromium 121+ 下会令自定义伪元素样式失效）。

## 7. 布局

桌面端是顶部导航加单一工作区（2026-08-27 用户指令参考 `CC Switch` 的顶栏方式；视觉令牌仍是本系统自有，不迁移其图标或资源）。

~~~text
╭──────────────────────────────────────────────────────────╮
│ Agent Switchboard   概览 供应商 通用设置 会话 备份 发现      │ ← 玻璃顶栏
├──────────────────────────────────────────────────────────┤
│ Dual Relay / 当前路由（仅概览）                              │
├──────────────────────────────────────────────────────────┤
│ 提供商工作区（单栏全宽）                                     │
│ 列表、表单、状态                                             │
│ 变更预览（显式请求后在列表正下方展开）                         │
├──────────────────────────────────────────────────────────┤
│ 最近校验与恢复历史                                          │
╰──────────────────────────────────────────────────────────╯
~~~

- 顶栏即自绘标题栏（2026-08-27 用户指令，参考微软电脑管家的融合方式；`decorations: false`）：左侧宝石应用图标 + 加粗品牌名为拖拽区，右侧水平导航项 + 置顶图钉快捷钮（契约见设置节「窗口始终置顶」行，与设置页复选框同一设置两个视图）+ 最小化 / 最大化 / 关闭三钮（关闭悬停转系统红）；玻璃材质，底部细分隔线。激活项用安静的蓝色调实心表面与 `600` 字重表达。最大化钮镜像实时窗口状态（2026-08-28）：窗口态显示单方块「最大化」，最大化态显示双方块「还原」，随窗口 resize 事件同步。2026-08-31 曾按当日指令短暂切换纯原生标题栏，用户对照微软电脑管家后当日拍板回退本方案——真原生（系统绘制、样式不可改）与「融入页头」互斥，wry 不支持 WebView2 WCO（wry#1650），最终形态即本条。
- Windows 系统托盘（2026-08-28 用户指令定稿）：应用启动即常驻。左键单击图标恢复并聚焦主窗口；右键弹出 Windows 原生菜单（菜单为 Agent Switchboard 自有结构，只呈现真实信息：一条「打开主界面」、两条禁用的 Claude / Codex 当前路由状态、分隔线与显式「退出」）。菜单状态必须来自现有真实配置状态，不得作为未确认的切换入口。托盘是可恢复性的硬前提：只有图标已成功创建，关闭策略才允许隐藏窗口；设置文件不可读时仍保活而非退出；菜单「退出」是唯一标记为显式的进程退出路径。
- 应用外壳必须始终可恢复（2026-08-28 用户指令）：顶栏与窗口控制不包入页面渲染树；工作区由错误边界隔离，任一业务视图渲染异常时仅替换为「界面未能加载」恢复面，托盘和关闭控制仍保留。
- 工作区占满顶栏以下的全部宽度；顶部路由卡不再与侧边栏分享横向空间。
- 会话管理为主从双栏：左栏是筛选后的会话列表，右栏只在选中后加载详情与对话历史。它不复用概览路由卡，也不把每条消息做成营销式彩卡；正文位于近实心内容面，长记录在详情内滚动。窄屏时上下堆叠，列表保留独立的固定高度滚动区。
- 变更预览没有独立检查器栏（2026-08-28 用户指令）：在显式请求后于供应商列表正下方、同一面板内展开，不再作为右侧栏或页面底部面板。配置工作区是稳定的中心区域，不得把差异做成次级页面。
- 窄屏（低于 `900px`）顶栏允许换行；表单和差异中不得出现水平滚动。
- 操作横幅是浮层，不在文档流内（2026-08-28 布局位移修复；同日修正；2026-08-31 职责收窄）：横幅层绝对定位于工作区顶部、`pointer-events: none`（横幅表面不拦截内容点击）、近实心底 + 模态投影；横幅内的操作控件（如旧档案清空入口）单独恢复指针事件，文字区域仍可穿透。横幅只承载需要用户决策的持续错误——目前唯一形态是 `profile-store-unsupported`（供应商存储格式不受支持，附「清空旧档案并重新开始」入口）；一次性操作反馈（失败、警告、切换 / 恢复 / 撤回结果）一律走 §8 全局通知，不再占用横幅。横幅的出现 / 消失绝不改变页面几何，滚动锚定不参与补偿。滚动容器（`.asb-main`、配置预览体）一律 `scrollbar-gutter: stable`，滚动条出现不得引起重排；配置预览体高度固定 `320px`，文件内容增减行不得改变模块外几何。

## 8. 组件语言

### 路由栏

`Dual Relay` 是唯一有意强调表现力的控件。它只出现在概览页顶部，路由条顶部为「当前路由」标题（2026-08-31 用户指令，复用面板标题令牌；文案与区域 `aria-label` 同源），下方由两张实心客户端路由卡组成，嵌在玻璃路由条内（玻璃内不放玻璃）。即使仅选中其中一个页面，也应同时展示两个客户端，因为二者关系是产品的核心事实。

### 提供商行

行结构为 CC Switch 式供应商卡（2026-08-27 用户指令 1:1 复刻；配色仍用本系统令牌）：

- 首字母头像方块（44px，中性底）、提供商名称，下方明细分两行（2026-08-28 用户指令）：首行为服务地址链接——显示主机名、行动蓝、悬停下划线，点击经 `opener` 插件在系统浏览器打开（wry 会拦截网页内新窗口，纯锚点无效；权限作用域仅允许 `http/https`），无地址时该行显示路由模式文案；次行为模型，仅有值时显示；
- 行首为拖拽把手（2026-08-28 用户指令，参考 CC Switch 的把手拖动排序；视觉为本系统令牌）：把手是行内唯一拖拽目标，指针移动超过 8px 才启动拖拽，行主体仍是点选；键盘以 Space 抬起、方向键移动、Space 落下完成排序。列表显示顺序的唯一所有者是 `state/profiles.json` 的档案数组——重排按客户端重写该客户端在数组中的片段，不引入每档案排序字段。拖拽中的行加强为行动蓝描边与投影，不缩放。
- 命中当前真实配置的卡片＝蓝调实心表面＋四色渐变描边的**强调档**流动（全不透明色停、4s 一周，减动态效果时静止）＋蓝调软投影，动作簇左侧同时显示「使用中」状态胶囊（文字承载状态，绝不只靠颜色）；
- 变更预览在显式请求前不占位；预览按钮是眼睛开关（2026-08-28 用户指令）——展开后图标变闭眼并带安静蓝色调激活底，再点收回，图标与 `aria-expanded` 共同承载状态，绝不只靠颜色。展开的预览内容渲染在**该行卡片内部、行线下方**（2026-08-28 用户指令，同日由面板底部移入行内）：卡片为纵向两段（行线＋展开区），展开区以细分隔线与行线分界、复用同一卡片表面；「变更预览」标题与「确认切换」主按钮位于展开区头部，其他行不受影响；
- 行尾为图标动作簇（预览 / 编辑 / 删除三个内联线性图标，装入胶囊形容器）：点击直接作用于该行、不改变选中；点选卡片本身只做选中；
- 「启用」主按钮（Play 图标 + 文字，2026-08-28 用户指令，参考 CC Switch 的主按钮形式；令牌为本系统自有）在所有非生效行渲染但默认不展示（2026-08-31 用户指令由「选中后才出现」改为悬浮即现），行被鼠标悬浮、键盘聚焦或选中时出现在动作簇左侧（显隐由样式表 `asb-row-activate` 拥有）：点击拉起变更预览，写入仍需「确认切换」确认弹窗，启用按钮绝不直接写配置。生效行不渲染启用按钮，状态由「使用中」胶囊表达。变更预览面板头部提供「确认切换」主按钮，复用与概览页路由控件相同的确认弹窗。
- 卡片常带细分隔边框，悬停转安静蓝色调实心表面；被选中的卡片换动态渐变描边、蓝调表面与蓝调软投影；
- 客户端切换是分段胶囊控件（2026-08-28 用户指令移至页面标题行、紧邻新建按钮左侧；此前独立成行）：共享一个容器底座，激活段为白色凸起卡片并带行动蓝描边，未激活段平贴透底；段内容为 OpenAI / Claude 品牌标（官方渠道获取：OpenAI 官方矢量、Claude App Store 官方应用图，商标归属各自公司，仅用于标识集成对象）；
- 新建供应商是页面标题行的圆形行动蓝 `+` 主按钮（同上，用户指令形式）；
- 变更预览只在显式请求后渲染——选中或切换筛选不得自动拉取差异；候选预览换选即失效，保证安全切换永远对应可见差异。
- 不得添加装饰性眉题、标语，或“安全 / 本地 / 快速”一类的标签云；
- 不得使用装饰性图标方块或彩色渐变卡片；
- 选中行使用安静的蓝色调实心表面与**静止档**四色渐变描边（半透明色停、6s 缓慢流动，同日用户指令由静态升级为动态），而非鲜亮填充；生效行将描边升级为**强调档**（全色停、4s）——强度与动效共同标记命中行。描边渐变、投影与时长令牌由 `tokens.css` 拥有（2026-08-28 用户指令：移植 spiralcoder 全局渐变色系——环为蓝 `#046aff`→紫→藕→金四停 0°/108°/216°/320°，静止档 `color-mix` 半透明、强调档全色；组件不落色值）。选中表面刻意不透明：描边画在 `border-box`，半透明表面会让边框渐变透过卡面显形为中心射线（当日移除的缺陷），spiralcoder 以不透明表面规避同一问题。选中行内的「启用」主按钮换装移植的按钮渐变（青→蓝→粉 88° 五停＋内嵌蓝光晕，悬停白光增强）——实心行动蓝在蓝调表面上同系相融，无法区分（同日用户指令）。

### 编辑视图

新建或编辑供应商时切换到独立界面（2026-08-27 用户指令，参考 `CC Switch` 的整页编辑方式；样式令牌仍为本系统自有）：页面内容替换为「返回按钮 + 标题 + 表单卡」，表单卡铺满工作区宽度，路由条与列表、变更预览全部隐藏，保存 / 取消仍在表单尾部。返回按钮是页面级唯一的图标按钮，文字标题说明当前是新建还是编辑。供应商档案一律为自定义服务（2026-08-28 用户指令，同日彻底落地）：不存在路由模式字段，服务地址必填；「官方登录」不再是档案种类——客户端自身的登录即为官方路由，概览与发现页对真实配置文件的「官方登录」观察展示不受影响。

### 表单与配置预览

- 标签应命名用户能控制的对象：“基础 URL”“模型”“API 密钥”“通用设置”。
- 下拉选择的唯一所有者是 `Select` 组件（2026-08-28 用户指令复刻 spiralcoder 的 `Radix Select` 结构，视觉为本系统令牌）：字段形触发器与 `asb-input` 同语言（细分隔边框、控制圆角、悬停转行动蓝），右侧弱化 chevron，未选中时显示占位文案；浮层为玻璃菜单材质（`78%` / `20px` 模糊、模态投影、`220ms` 淡入缩放进入），选项行 `40px` 触控目标、键盘高亮为安静蓝调表面、当前项右置勾选指示，方向键与 `Escape` 可达。一切下拉选择消费该组件，不再使用原生 `<select>`。
- 避免使用重复可见字段标签或只是在叙述表单的卡片级副标题。
- 路径、`TOML`、`JSON` 和差异使用代码字体与接近实心的背景。
- 配置文件预览逐行换行显示：行号列固定，长行（如超长服务地址）在文本列内折行，任何情况下不得出现横向滚动（2026-08-28 用户指令）。
- 预览渲染各有一个唯一所有者（2026-08-28 用户指令统一化、同日拆分）：`CodePreview`（文件视图：行号 + 令牌着色 + 换行）与 `DiffView`（差异视图：键 + 旧值 → 新值）。一切预览表面（通用设置文件预览、供应商变更预览、备份差异）消费这两个组件，不得另行重建标记或着色逻辑。
- 差异视图为红绿语义（2026-08-28 用户指令）：旧值红色文字配浅红 tint 芯片（`--asb-danger` + `--asb-diff-remove-bg`），新值绿色文字配浅绿 tint 芯片（`--asb-diff-add-text` + `--asb-diff-add-bg`，明暗两套令牌），「移除」红色加粗；箭头与「移除」文字保证状态不只靠颜色可读（§10）。旧值缺失（`（无）`）保持中性弱化——无旧值即无移除，不得染红。芯片为近实心底上的着色小块，绝不引入背景模糊。
- 优质打印候选全文归一切预览表面（2026-08-28 用户指令接入供应商变更预览）：`FilePreview.content` 是唯一 UI 来源——执行器只把**按 TOML / JSON 键路径脱敏后的展示副本**交给界面；原始候选文本仍只在执行器内用于 `rendered_hash`、语法校验与原子写入。因此确认仍绑定原始候选哈希，界面绝不显示 API key 或任何宿主既有密钥。供应商变更预览不再平铺「保持不变的设置」键列表：被保留的宿主键在候选全文中原位可见；面板顶部路径标题由文件视图自带的文件条承担，避免重复。`SwitchPreview.preserved` 旧字段已随迁移删除——任何预览表面不得重建宿主键平铺列表，一切配置文本一律经 `CodePreview` 渲染。
- 密钥必须渲染为稳定的脱敏令牌，绝不显示部分可见的值。
- 错误应说明确切字段和恢复操作；不应道歉。
- 时间展示的唯一所有者是 `Time` 组件（2026-08-28 用户指令统一化）：格式 `YYYY-MM-DD HH:MM:SS` 本地时区，**不带时区偏移后缀**（时钟读数本就是本地时间，`（UTC+8）` 标注已移除）；`timeLabel` 只是格式化纯函数，扫描测试锁定组件外无任何直接消费者（概览匹配态与上次切换、备份历史、端点验证、撤销/恢复确认全部经组件接入）。
- 端点验证结果行以文字颜色承载结论（2026-08-28 用户指令）：可达＝安全绿（`asb-ok-text`），不可达＝警示红（`asb-fail-text`，取 `--asb-safe` / `--asb-danger`）；「可达 · HTTP 状态 · 延迟」与「不可达」文字同时在场，结论绝不只靠颜色可读（§10）。

### 通用设置（2026-08-28 用户指令）

- 页面为 `Codex` / `Claude` 双 `Tab` 切换，`Tab` 复用供应商页的品牌图标分段样式。
- 每个勾选行对应一条官方配置参考中的真实配置行：勾选 = 该行写入通用配置文件（`Codex` 为 `~/.codex/config.toml`，`Claude` 为 `~/.claude/settings.json`），取消勾选 = 该行移除；写经由切换执行器完整事务（加锁、备份、原子替换、写后校验）即刻生效。
- 行内以等宽字体展示确切代码行；勾选状态反映配置文件的真实行状态，不是叠加补丁的期望值。
- 目录完整覆盖官方配置参考中的用户偏好选项（2026-08-28 用户指令），分两类控件、按分组呈现：布尔选项为复选框；多值选项为档位控件——推理强度保留专用滑块，其余为分段药丸（共享底座 + 白色凸起激活段 + 行动蓝描边，与客户端 `Tab` 同语言；`role="radiogroup"`，键盘可达）。分组顺序：`Codex`「模型行为 / 安全与审批 / 隐私与数据 / 终端界面 / 工具与功能」，`Claude`「模型行为 / 界面与交互 / 文件与 Git」。值签悬停显示将写入的确切行；文件值超出目录档位时原样展示并标注「默认（值）」。
- 推理强度滑块为 `spiralcoder` 完整复刻：胶囊轨道、能量渐变填充（`--asb-gradient-energy`，安全绿→行动蓝→Claude 紫，本系统第二处获准渐变）、填充内九颗微光粒子、刻度与白色滑钮，动效随 `prefers-reduced-motion` 关闭。
- 选项目录唯一所有者是 `asb-core::ownership` 的 `common_toggles` / `common_choices` / `common_groups`（开关的 applied 值、档位的合法值、分组顺序与校验同源）；界面不得自建键列表。模型行为参数（推理强度 / 推理摘要 / 回复详细度）全部归通用设置，档案编辑器的运行参数只保留上下文窗口；模型路由仍归供应商档案。
- 目录刻意不收录：供应商路由与凭据（归供应商页与客户端自身）、`MCP` / 权限 / 沙箱策略表与安全例外键（超出安全配置管理器边界或为复杂表）、路径与自由文本键、宿主内部状态键。

### 设置（2026-08-28 用户指令）

- 顶层「设置」页只管理 Agent Switchboard 自身，不混入 Codex / Claude Code 的通用设置；当前组为「外观」、「窗口与托盘」、「性能」与「软件更新」。
- 「界面主题」为 `跟随系统 / 浅色 / 深色` 分段单选：显式浅色压过 Windows 深色、显式深色压过 Windows 浅色，跟随系统维持媒体查询；「动态效果」为 `跟随系统 / 减少动态效果`，后者停止现有路由、滑块和控件强调动画。
- 「点击关闭按钮时」为分段单选：`最小化到托盘`（默认，关闭后继续运行并由托盘恢复）或 `退出应用`（关闭即结束进程）；「窗口始终置顶」为即时复选框。置顶的 native 调用先于落盘，失败不产生“已保存但未生效”的半状态。顶栏置顶快捷钮（2026-08-31 用户指令）与该复选框是同一设置的两个视图：图钉钮位于导航项与窗口三钮之间，未置顶灰色图钉 / 置顶行动蓝并携带 `aria-pressed`，提示与可及名「置顶窗口 / 取消置顶」；点击经同一 `set_app_settings` 完整对象路径持久化（先 native 后落盘），设置未加载前不渲染，保存失败保持原视觉状态。
- 「启用硬件加速」控制 WebView2 的 GPU 渲染；因浏览器参数仅在 WebView 创建时读取，保存后必须完整重启应用才会生效。关闭时在 Wry 默认浏览器参数上追加 `--disable-gpu`；若窗口已有显式参数则保留它们并追加该开关。
- 「软件更新」（2026-08-31 用户指令）为手动检查分组：一条信息行 +「检查更新」次级按钮，结果原位展示——未检查时说明来源，检查后显示「已是最新版本」或「发现新版本 {tag}」并附当前版本与检查时间（本地时区）。发现新版本时同一行追加「打开下载页面」主按钮（经 opener 插件仅放行 http/https），顶栏导航与置顶钮之间出现行动蓝下载箭头指示钮（提示与可及名「发现新版本 {tag}」），点击跳转设置页；无新版本或未检查时不渲染。检查只读 GitHub 发布页接口，不下载、不安装、不自动重试，失败走全局错误通知。
- 设置值以完整严格对象原子保存到应用数据 `state/settings.json`：`closeBehavior`、`theme`、`motion`、`alwaysOnTop`、`hardwareAcceleration` 全部必填；恰好为上一版完整四字段对象的既有文件，会原子补入 `hardwareAcceleration: true` 后立即改写，以保留原有 GPU 默认行为；其他不完整形态、未知字段和非法枚举值都明确拒绝。绝不写入客户端配置文件、供应商档案或通用补丁。
- 设置组使用紧凑实心信息行，与通用设置的分段语言一致；不会显示虚构的配置代码行或候选文件预览。

### 会话管理（2026-08-28 用户指令）

- 顶层「会话」页只覆盖 `Codex` 与 `Claude Code`，与产品支持范围一致。列表支持关键词与客户端筛选，默认由最近活跃时间排列。
- 选中一条会话才加载其对话历史；详情显示客户端、会话 ID、最近活跃、已记录工作目录和恢复命令。会话源路径不在界面或前端契约中暴露。
- 行操作包括「在命令提示符中恢复」、复制恢复命令和（可得时）复制工作目录。「在命令提示符中恢复」以新窗口启动受控的客户端恢复命令，工作目录可用时直接作为进程目录传入；不提供删除会话、修改历史、会话数据库写入或任意命令执行入口。
- 来源缺失或不可读是行内提示，不替代其它客户端的可读结果，也不伪造空数据。

### 操作

| 操作 | 处理方式 |
| --- | --- |
| 安全切换 | 行动蓝填充，白色标签 |
| 查看差异 | 安静的玻璃 / 实心次级操作 |
| 恢复备份 | 中性描边操作，必须确认 |
| 打开备份文件夹 | 中性描边操作，直接唤起系统资源管理器，无确认 |
| 撤回上一次切换 | 系统红色填充，白色标签，必须确认（2026-08-28 用户指令） |
| 删除提供商 | 系统红色用于入口与确认流程 |

### 全局通知（2026-08-31 用户指令，复刻 spiralcoder）

结构复刻 spiralcoder 的 toast 系统，视觉为本系统令牌。`use-toast.ts` 是唯一契约所有者（模块级 store，任意层可直接调用 `toast()`，无需 context）；`Toaster` 在应用根挂载一次。

- 栈：视口左上角悬浮（2026-08-31 用户指令，避开顶部居中的导航），最多同屏 2 条，超出淘汰最旧；错误置顶且绝不因新通知被挤出。栈本身 `pointer-events: none`，通知表面恢复指针事件。
- 种类与图标：info / success / warning / error 四种手绘线性图标，色调分别为行动蓝 / 安全绿 / 警告橙 / 系统红；error 用 `role="alert"`，其余 `role="status"`。
- 自动关闭：info 4.5s、success 3.2s、warning 6s、error 10s（2026-08-31 用户指令设定错误关闭时长，比警告更长以留足阅读后端错误信息的时间）；`durationMs: 0` 可对单条强制手动关闭。
- 暂停契约：悬停、聚焦或页面隐藏时暂停计时并保留剩余时长，离开后继续。
- 材质：玻璃菜单面（`--asb-glass-menu` + 菜单级模糊）、模态投影、顶缘高光；标题半粗、副文转弱（仅有副文时保持实色）；右上关闭钮 40px 触控目标。
- 动效：进入 240ms 上移淡入、退出 180ms 上移淡出缩小；减动态效果时经令牌归零即时呈现。
- 层级：`z-index: 70`，位于表单确认页与工具提示之上。
- 与操作横幅的分工（2026-08-31 接入定稿）：横幅（§7）只承载带决策入口的持续错误；toast 是全部一次性操作反馈的唯一通道——写操作失败（error，含 message）、切换 / 恢复备份 / 撤回的结果（成功为 success「已切换到「X」/ 已恢复备份 / 已撤回上一次切换」，有警告时合并为一条 warning，标题带警告条数、每条警告一行）、通用设置写入的警告（成功本身以控件状态呈现，不通知）。发现页 CC Switch 导入结果保留面板内嵌展示（含未导入明细），不发全局通知。

## 9. 动效与状态

- 按下：在 `120ms` 内缩放至 `0.98`。
- 路由节点移动：`220ms`、标准缓动，仅在确认后发生。
- 面板进入：`240ms` 向上淡入并平移。
- 悬停：背景和细分隔线变化；不得使用卡片抬升动画。
- 焦点：带可见偏移的 `2px` 行动蓝圆环。绘制类选择控件（复选框、分段药丸、滑块）的圆环仅在键盘导航时显示——复选 / 单选 / 滑杆输入被鼠标点击时原生匹配 `:focus-visible`，环会包裹每次点击，故由输入模式标记（根元素 `data-focus-source="key"`）区分：键盘导航键按下时标记、任何指针按下时清除；表单文本输入的焦点环不受影响（2026-08-28 用户指令）。
- 减少动态效果：移除路由移动和面板平移；状态变化保持即时且可见。

## 10. 无障碍与性能

- 最小交互目标为 `40px`；仅图标按钮为 `44px`。
- 所有文字均达到面向 `WCAG` 的对比度，包括紫色身份标签。
- 绝不能只用颜色表示启用状态：还需包含文字、圆点或轨道位置。
- 可见的同时 `backdrop-filter` 层数不得超过四层。
- 低功耗条件和请求减少动态效果时，禁用高成本的环境背景移动。
- 提供键盘导航、可见焦点环，并使用 `Escape` 关闭面板和对话框。

## 11. 应当与禁止

**应当**

- 让配置数据保持视觉焦点。
- 用留白、细分隔线和材质深度建立层级。
- 让简短标题与实际设置 / 值共同说明面板。
- 让下一项不可逆操作清晰无误。
- 在相关页面始终显示启用的路由。

**禁止**

- 使用彩虹渐变、动态粒子、霓虹光晕或通用指标卡片。
- 模糊代码块、差异文字或表单文字。
- 用玻璃效果代替信息层级。
- 将标签堆、眉题、通用副标题或解释性填充文案用作视觉装饰。
- 在组件中增加第二种操作色或新的圆角尺度。
- 将路由轨道变成装饰性动画。

## 12. 设计评审说明

对于“Apple + 高斯模糊”，最初直觉往往是全屏磨砂玻璃。这会让配置管理器变得含糊且令人疲劳。本系统有意只把视觉强调用在一个有意义的元素——`Dual Relay`——其余配置与审计界面则保持平静、实心且清晰。同样的克制也适用于文字：移除装饰性标签和副标题文案后，信息层级会更清楚，而非更弱。
