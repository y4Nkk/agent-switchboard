# Agent Switchboard 设计系统

**版本：** 0.1
**状态：** 实施前已批准的视觉基线
**方向：** `Frosted Relay` — 面向双客户端配置控制台的 Apple 式层级与克制的压纹实体表面。

## 1. 产品与视觉主张

**主题：** 一个安全管理 `Codex` 与 `Claude Code` 提供商配置的本地桌面应用（`Windows` / `macOS` / `Linux`）。

**受众：** 一位需要了解当前启用内容、即将变更内容及撤销方式的技术型用户。

**唯一任务：** 让当前启用配置和切换后果能够立即被理解。

这不是营销页面，也不是通用 `SaaS` 仪表盘。主视觉对象不应是横幅图，而应是真实路由状态：两条客户端轨道与各自启用的提供商节点。主窗口切换入口在供应商页的变更预览，托盘提供显式快捷切换；二者共用同一执行器，路由栏保持纯状态面。

## 2. 文字是信息，而非装饰

界面文字必须配得上所占空间。产品管理的是配置，因此空白区域往往比一叠用来强调区域重要性的标签更合适。

- 每个工作区域最多只有一个任务标题。仅当辅助行会改变用户下一步决策，或解释不明显的风险时才添加。
- 不得只为让面板显得经过设计而添加眉题、“功能”副标题、营销短语或配置主题填充文案。
- 徽标或标签只可说明真实的当前属性：启用状态、校验结果、警告、筛选条件或客户端身份。它不是视觉分隔符，也不能代替间距。
- 不得在标题下放置成组的状态类标签。唯一有意义的状态应紧邻它修饰的控件或值。
- 字段标签、校验信息和配置路径注释仍是必要的功能性文字。保持简短，并紧邻其说明对象。
- 采用删除测试：移除一处文字后，若设置、操作、状态、警告或决策并未变得更难理解，就删除它。

`Dual Relay` 与配置内容提供层级。其余视觉工作必须由字体、留白、细分隔线和材质深度完成。

## 3. 参考解读

`SpiralCoder` 提供了有价值的实现纪律：

- 有文档记录的设计系统；
- 一个令牌所有者，而非组件局部的视觉常量；
- 中文界面文字使用 `MiSans`，代码使用专用字体；
- 按层级使用实体表面、细边和内高光；
- 低阴影、高可读性的配置表面。

`Agent Switchboard` **不**继承 `SpiralCoder` 的蓝粉品牌渐变、面向儿童的图像、大型展示区或装饰性粒子效果。本产品处理配置和凭据，因此平静与可审计性比表现力强的颜色更重要。

### 界面来源

`CC Switch` 和 `Codex++` 是后端操作参考，而非视觉参考。`Frosted Relay` 的布局、令牌、组件、交互设计、图标与界面文案均由本文件为 `Agent Switchboard` 创建。不得迁移任一项目的页面、组件结构、资源或文字。

## 4. 视觉标识：`Dual Relay`

产品令人记忆的元素是双客户端路由本身。概览页顶部以两张并排的路由卡呈现两条客户端轨道的当前状态；路由卡仅在概览出现，不再常驻其他页面（用户于 2026-08-27 指令）。卡片视觉完整复刻自 `spiralcoder` AI 权益工作台的模型服务卡（用户于 2026-08-27 指令），是本系统获准的渐变表面之一（另一处为通用设置滑块的能量渐变，2026-08-28 用户指令）：

- **已启用卡**：客户端身份渐变加同色底部深罩——`Codex` 为蓝→浅蓝→暖桃流（`--asb-route-active-gradient-codex`），`Claude` 为紫→浅薰衣草→暖桃流（`--asb-route-active-gradient-claude`，2026-09-03 用户指令由两卡共用渐变改为各归身份）——星光激活层 `StarlightLayer` 携同身份星光点阵与右下辉光随 `is-on` 同步浮现（`Codex` 冷白星光 + 蓝辉光，`Claude` 薰衣草星光 + 紫辉光；canvas 渲染，顶部渐隐 + 左上内容安全区，色调令牌 `--asb-starlight-{cool,violet}-*-rgb`），白色文字，底部为实体三值条。整卡仅此处允许投影（`--asb-route-shadow`）。
- **未加载卡**：近实心表面，中性描边与灰字；星光与辉光随激活态退场（2026-09-03 用户指令改用 spiralcoder 推荐卡的亮起/安静姊妹语言，不再常驻蓝紫星光）。
- `Codex` 卡以行动蓝整边描边标识，`Claude` 卡用紫色整边描边；渐变、星光与辉光同样各归客户端身份（2026-09-03 用户指令），颜色仅表示身份，不形成第二套操作色。
- 路由卡不设状态徽标（2026-09-02 用户指令）：启用态由渐变与星光表达，未加载态由供应商名与字段值回落为「未加载 / —」表达，不靠颜色作唯一区分。
- 路由栏是纯状态面，不放操作按钮（2026-09-02 用户指令）：「查看变更」与「安全切换」入口整体移除；切换只从供应商页的变更预览进入，经「确认切换」与同一确认弹层执行。
- 卡内只呈现真实路由状态：客户端名左侧的品牌标瓦片（与供应商页分段控件同源的官方图标，装在固定浅色圆角瓦片上，深色渐变面与深色主题下均保持可读）、生效供应商名、模型、服务地址、接入方式。禁止用量统计、迷你图表或任何虚构数据；本地会话 token 消耗仅在独立「用量」页呈现，并明确不代表供应商剩余额度。
- 路由卡不读取其后的像素；值条和未加载卡均使用实体表面，杜绝材质嵌套。
- 星光激活层的唯一所有者是全局组件 `StarlightLayer`（`components/experience/StarlightLayer.tsx`，2026-09-03 用户指令复刻 `spiralcoder` 今日推荐卡）：星光矩阵与右下辉光作为一个 `aria-hidden` 装饰层随宿主的激活旗标同步浮现（`data-active`，`--asb-motion-node` 过渡），层自裁剪、指针穿透；宿主只决定激活与否与色调档位。配色按页与客户端划分（2026-09-03 用户指令）——`cool` 冷白星光 + 蓝辉光归概览 `Codex` 卡，`violet` 薰衣草星光 + 紫辉光归概览 `Claude` 卡（卡片本身就是身份面，与描边同源），`warm` 琥珀星光 + 暖金辉光归供应商选中行（身份中立：紫在此冒充 `Claude` 身份、绿撞安全语义，故不用）。色调令牌与辉光渐变唯一所有者 `tokens.css`（`--asb-starlight-{cool,violet,warm}-*`），辉光由层上的 `data-variant` 选取。粒子由 `MatrixStarlightCanvas` 以 30fps 上限绘制；减少动态效果时绘制单帧静止画面（2026-08-28 用户指令：粒子升级为 `spiralcoder` 最新星光矩阵实现）。
- 概览的「配置状态」模块与路由卡同构（2026-08-28 用户指令）：同一面板内每个客户端一张实体状态卡，头部为品牌标瓦片加客户端名与带圆点的状态胶囊（配置正常 / 语法错误 / 未找到配置文件 / 读取失败，颜色不作唯一表达；品牌标瓦片与路由卡同款，2026-08-28 用户指令），下方为带标签的字段行——配置文件、当前服务、匹配状态、上次切换、范围警告、写入锁；遗留锁的清理按钮位于所属卡内，不再使用整宽裸文本键值堆。
- 概览在配置状态之后放置独立的「Codex 重置信号」面板（2026-09-04）：打开时只读取最后一次成功的本地公开快照，并以「本地缓存」状态标识；用户显式刷新后，以左侧的本次检查当日结论和右侧账本呈现最近完成信号、公开 feed 的最近检查与下一次预计；已完成信号同时读取 `events`、`fulfilledSchedules` 与 `manualCompletions`，并明确区分全局重置、重置卡发放与其他完成信号。Tibo 最近的重置相关动态留在结论区。刷新失败不得清空已有快照，需说明仍在显示上次成功数据；刷新成功而缓存写入失败也须说明结果无法在下次启动保留。它不属于 `Dual Relay` 路由卡，不显示会话管理、账号用量或本地凭据；必须清楚标注为 Codex Runway 的非官方公开信号，并显示可点击的公开 feed URL（经系统浏览器打开），不能声称代表当前账号额度或 Tibo 的完整推文时间线。Codex Radar 替换数据源前必须取得其接口授权。
- 公开 feed 面板之后放置「Codex 官方额度重置」面板（2026-09-02）：与本机 Codex 官方登录同源的账号级数据，与上方的非官方公开信号相互独立、各自标注来源，互不冒充。打开时只读取持久化基线的最后一次成功读数并标「本地缓存」；用户显式刷新才读取官方 usage 接口，失败保留已显示数据并说明原因。每个服务端限额窗口经 `QuotaWindowsTable` 显示为「窗口 / 已用 / 重置 / 占比」表格行，声明重置时间的窗口在「重置」列附「<时间> · 约 N 天 M 小时后」的渲染时倒计时（无计时器，刷新即重算）。事后重置检测由连续两次成功读数自动比对得出，仅观察 7 天窗口：用量下降或声明重置时间变更即判定重置发生，先前声明时间尚未到达的判「提前重置」、否则判「例行重置」；检测不产生预告——提前公告仍只属于上方公开 feed。基线保存为 app-data `state/codex-quota-baseline.json`，仅含规范化读数与短不可逆账号摘要：不随档案新增、编辑、删除或档案库重置失效（`auth.json` 是机器级单一登录），登录账号变更时清空检测历史以免误报。供应商页官方额度面板仅在原「重置 <时间>」后追加同一倒计时，不新增面板。
- 概览最底部放置只读「运行环境」面板（2026-09-04）：仅显示 Agent Switchboard 自身的应用版本、构建模式、运行平台/架构、监听端口、响应情况及应用数据目录。浏览器开发模式显示本机桥接的回环地址与健康检查 HTTP 状态；桌面壳明确显示「无 TCP 端口」与桌面协议响应，不能把桌面协议伪装为网络服务。它不读取客户端配置、凭据、账号信息或用量，也不提供写入操作；六项以低强调度的实体信息块呈现，长路径可换行审阅。

## 5. 令牌计划

运行时令牌值最终将集中在一份样式表中，拟定为 `src/styles/tokens.css`。组件只能消费语义令牌。

### 核心色板

| 令牌 | 名称 | 值 | 作用 |
| --- | --- | ---: | --- |
| `--asb-ice` | 极光冰白 | `#F6F7FB` | 浅色画布与高光 |
| `--asb-ink` | 板岩墨黑 | `#14161B` | 主文字与高对比度内容 |
| `--asb-current` | 深流蓝 | `#071725` | 深色画布与环境背景 |
| `--asb-action` | 行动蓝 | `#0A84FF` | 按钮、焦点与 `Codex` 身份 |
| `--asb-claude` | `Claude` 紫 | `#8B7CFF` | `Claude` 身份与非操作性标签 |
| `--asb-safe` | 安全绿 | `#30D158` | 已验证与已完成状态 |

警告与错误仅使用 Apple 式系统语义色。它们不得成为第二套品牌色板。

### 字体

| 角色 | 字体栈 | 用途 |
| --- | --- | --- |
| 展示文字 | `var(--asb-font-user)`, `Segoe UI Variable Display`, `MiSans`, `system-ui` | 页面标题与当前提供商 |
| 界面文字 | `var(--asb-font-user)`, `Segoe UI Variable`, `MiSans`, `system-ui` | 标签、表单与导航 |
| 代码 | `Maple Mono`, `Cascadia Mono`, `ui-monospace` | `TOML`、`JSON`、路径和配置差异 |

- 界面默认字体为 `Noto Sans SC`（2026-08-31 用户指令）：`OFL` 许可，经 `@fontsource/noto-sans-sc` 以 400 / 600 / 700 三个字重随应用打包（`main.tsx` 引入，子集按 `unicode-range` 懒加载），任何机器上都可真实渲染，不依赖系统安装。字体名到 `CSS` 值的引号转换唯一所有者是 `src/lib/font-family.ts`。
- `--asb-font-user` 的运行时值由设置「界面字体」覆写（所有者 `useAppSettings`，与 `data-theme` 同一应用点）；代码与配置预览不随界面字体变化，保持等宽栈。
- 未作出单独的许可证决定前，不得打包 `SF Pro`。
- 标题使用 `600` 字重，字距紧凑但易读。
- 正文使用 `15–16px` 与 `400` 字重。
- 配置数据使用等宽数字与代码字体。
- 展示字体必须克制：工作界面中不得出现超大的营销式标题。

### 形状与间距

| 令牌 | 值 | 用途 |
| --- | ---: | --- |
| `--asb-radius-window` | `28px` | 主外壳与大型面板 |
| `--asb-radius-panel` | `22px` | 主面板 |
| `--asb-radius-control` | `14px` | 输入框与辅助控件 |
| `--asb-radius-action` | `12px` | 主操作和破坏性操作按钮 |
| `--asb-radius-check` | `5px` | 复选框等小型勾选控件 |
| `--asb-space-unit` | `4px` | 原子间距单位 |

结构间距使用 `8`、`12`、`16`、`24`、`32` 和 `48px`。操作控件不自动采用药丸形；完整药丸形仅保留给紧凑状态控件和启用的提供商节点。

## 6. 材质系统

所有表面直接绘制自身的颜色与渐变，不采样或重绘其后的像素。

| 材质 | 用途 | 表面 |
| --- | --- | --- |
| 环境背景 | 应用背景 | 直接绘制的柔和蓝色径向流场 |
| 轨道面 | 顶栏与路由栏 | `--asb-surface-rail`、细分隔线与内高光 |
| 菜单面 | 菜单、工具提示与通知 | `--asb-surface-menu`、模态投影 |
| 抽屉面 | 差异确认与模态流程 | `--asb-surface-sheet`、细分隔线与内高光 |
| 内容面 | 表单、表格、代码预览、差异主体 | `--asb-content` 或 `--asb-content-muted`，高对比度文字 |

实现规则：

- 将环境背景渲染为真实的、带 `aria-hidden` 的 DOM 层。用少量大型径向光源直接绘制；不得出现可见的伪元素装饰。
- 表面使用 `1px` 白色或中性色细分隔线，以及一道轻微的内嵌高光。
- 阴影仅用于模态框或抬升的菜单。卡片默认不得悬浮。
- 主窗口只有这一套实体表面实现；禁止 CSS `filter`、`backdrop-filter` 和浏览器能力后备分支。独立托盘窗口使用操作系统提供的单层材质（见系统托盘交互规则），不在内容内叠加模糊。
- 尊重系统浅色和深色模式。浅色模式使用极光冰白；深色模式使用深流蓝，绝不使用纯黑。

滚动条（2026-08-28 用户指令，自有设计）：全局以「墨线滚动条」替换 WebView2 默认滚动条——`10px` 槽位、轨道与按钮全透明（无凹槽底色），滑钮经透明描边内缩为 `4px` 墨色药丸线并保持全槽拖拽命中区，悬停加深、拖拽最强，最短 `32px`；深浅模式各用一套基于正文墨色的 tint，绝不引入第二强调色。规则唯一所有者是 `base.css` 的 `::-webkit-scrollbar` 全局块，值由令牌（`--asb-scrollbar-*`、`--asb-radius-pill`）提供，覆盖工作区、配置预览与文本域等一切滚动表面。不得混用标准 `scrollbar-width` / `scrollbar-color`（Chromium 121+ 下会令自定义伪元素样式失效）。

## 7. 布局

桌面端是顶部导航加单一工作区（2026-08-27 用户指令参考 `CC Switch` 的顶栏方式；视觉令牌仍是本系统自有，不迁移其图标或资源）。主导航的当前页唯一状态是 `aria-current="page"`：默认项为弱化 `500` 字重，悬停仅转主文字色，当前项为主文字 `700` 字重；三者都不使用填充底、描边或下划线。键盘焦点独立使用行动蓝焦点环，绝不伪装成当前页状态（2026-09-02 用户指令）。

~~~text
╭──────────────────────────────────────────────────────────╮
│ Agent Switchboard   概览 供应商 通用设置 会话 日志 备份 发现 │ ← 实体顶栏
├──────────────────────────────────────────────────────────┤
│ Dual Relay / 当前启用配置（仅概览）                           │
├──────────────────────────────────────────────────────────┤
│ 提供商工作区（单栏全宽）                                     │
│ 列表、表单、状态                                             │
│ 变更预览（显式请求后在列表正下方展开）                         │
├──────────────────────────────────────────────────────────┤
│ 最近校验与恢复历史                                          │
╰──────────────────────────────────────────────────────────╯
~~~

- 顶栏即自绘标题栏（2026-08-27 用户指令，参考微软电脑管家的融合方式；`decorations: false`）：左侧宝石应用图标 + 加粗品牌名为拖拽区，右侧水平导航项 + 置顶图钉快捷钮（契约见设置节「窗口始终置顶」行，与设置页复选框同一设置两个视图）+ 最小化 / 最大化 / 关闭三钮（关闭悬停转系统红）；实体轨道材质，底部细分隔线。激活项用安静的蓝色调实心表面与 `600` 字重表达。最大化钮镜像实时窗口状态（2026-08-28）：窗口态显示单方块「最大化」，最大化态显示双方块「还原」，随窗口 resize 事件同步。2026-08-31 曾按当日指令短暂切换纯原生标题栏，用户对照微软电脑管家后当日拍板回退本方案——真原生（系统绘制、样式不可改）与「融入页头」互斥，wry 不支持 WebView2 WCO（wry#1650），最终形态即本条。
- 系统托盘（2026-09-05）：保留系统图标，删除原生菜单。左右键均开关同一个独立 `tray` 窗口，Esc 或点击外部隐藏；鼠标移出不关闭。面板宽 360 逻辑像素，外轮廓只由原生窗口裁切；内容铺满客户区，不另画圆角、描边或外侧留白。关闭会给无装饰窗口添加白边的 Tauri shadow；Windows DWM 请求无描边圆角，macOS Popover 材质半径由窗口配置拥有。系统不支持圆角时保持同一无框矩形表面，不另建后备窗口。使用单层系统材质与高对比度文字；不套用主界面导航，不加载主界面业务树。Windows 使用 Acrylic，macOS 使用 Popover 材质；Linux 通过 StatusNotifierItem 激活同一自定义界面，不提供 DBusMenu。Wayland 的全局定位由合成器控制，不承诺贴合图标。
- 托盘按 Codex / Claude Code 分组，显示供应商名、模型和已有缓存额度及时间；真实生效档案勾选且不可重复切换。点击明确的「切换」行先生成既有预览，再携带哈希进入同一原子切换执行器；托盘不构造配置、不传递 API 密钥，也不另起用量轮询。底部为「管理供应商 / 退出」，标题栏可打开主界面。读取失败保留可用内容并显示原因；操作失败保持面板以便重试。
- 托盘定位与显示状态由 Rust 唯一拥有：按图标位置选择显示器，以该显示器缩放比例统一到物理像素，优先向可用空间展开，再限制在工作区内；覆盖负坐标、副屏、任务栏方向及高 DPI。内容准备好、尺寸及位置设置完成后才显示，内容增高超过工作区时只滚动列表。主题、字体和减少动态效果与主窗口消费同一外观函数。只有托盘入口可用时关闭策略才允许隐藏主窗口；不保留原生恢复菜单。
- 应用外壳必须始终可恢复（2026-08-28 用户指令）：顶栏与窗口控制不包入页面渲染树；工作区由错误边界隔离，任一业务视图渲染异常时仅替换为「界面未能加载」恢复面，托盘和关闭控制仍保留。
- 工作区占满顶栏以下的全部宽度；顶部路由卡不再与侧边栏分享横向空间。
- 会话管理为主从双栏：左栏是筛选后的会话列表，右栏只在选中后加载详情与对话历史。它不复用概览路由卡，也不把每条消息做成营销式彩卡；正文位于近实心内容面，长记录在详情内滚动。窄屏时上下堆叠，列表保留独立的固定高度滚动区。
- 日志页是应用自身运行事件的只读账本：顶部「记录级别」经应用设置的唯一保存路径选择「调试 / 信息 / 警告 / 错误 / 静默」阈值；「静默」停止后续写入，绝不作为表格事件。已记录事件可按「全部 / 调试 / 信息 / 警告 / 错误」筛选，并可手动刷新或显式打开应用日志文件夹；主体为时间、级别、闭合事件名与失败时的稳定错误码表格。打开操作由后端解析并交给系统文件管理器，路径不进入渲染层。它只读取本应用受限日志文件中的结构化记录，不读取配置写入历史、客户端配置、文件路径、密钥、请求正文、响应正文、异常消息或调用栈；窄屏时记录级别、筛选、刷新与打开控件纵向排列，表格在自身容器内横向滚动。
- 变更预览没有独立检查器栏（2026-08-28 用户指令）：在显式请求后于供应商列表正下方、同一面板内展开，不再作为右侧栏或页面底部面板。配置工作区是稳定的中心区域，不得把差异做成次级页面。
- 窄屏（低于 `900px`）顶栏允许换行；表单和差异中不得出现水平滚动。
- 操作横幅是浮层，不在文档流内（2026-08-28 布局位移修复；同日修正；2026-08-31 职责收窄）：横幅层绝对定位于工作区顶部、`pointer-events: none`（横幅表面不拦截内容点击）、近实心底 + 模态投影；横幅内的操作控件（如旧档案清空入口）单独恢复指针事件，文字区域仍可穿透。横幅只承载需要用户决策的持续错误——目前唯一形态是 `profile-store-unsupported`（供应商存储格式不受支持，附「清空旧档案并重新开始」入口）；一次性操作反馈（失败、警告、切换 / 恢复 / 撤回结果）一律走 §8 全局通知，不再占用横幅。横幅的出现 / 消失绝不改变页面几何，滚动锚定不参与补偿。滚动容器（`.asb-main`、配置预览体）一律 `scrollbar-gutter: stable`，滚动条出现不得引起重排；配置预览体高度固定 `320px`，文件内容增减行不得改变模块外几何。

## 8. 组件语言

- Tooltip 只解释当前可聚焦或可悬停的控件；触发器收到鼠标或键盘点击后，提示立即关闭，并在指针离开或焦点移走前不得因同一动作重开。该交互只由全局 `Tooltip` 组件拥有，业务按钮不得各自维护 Tooltip 状态（2026-09-02 用户指令）。
- 数据表格只由全局 `Table` 组件渲染（2026-09-02）：`columns / rows / rowKey` 列契约是唯一表格形态，视觉复刻 `spiralcoder` 数据表（2026-09-02 用户指令；令牌为本系统自有）——通栏弱化表头带、其下是无框大间距行、悬停行提亮，所有列一律左对齐（2026-09-02 用户指令）；表格自持 `1px` 细边框与控制圆角的 Apple 式圆角框（2026-09-02 用户指令），表头带与末行悬停面裁切到自身圆角，日志滚动区与工作台结果容器不再叠加第二层框；滚动区仍归各模块自身容器。用量模块的显示全部接入：备份历史、运行日志、CC Switch 导入扫描、供应商用量读数与查询工作台结果（共享 `UsageReadingsTable`），Codex 官方订阅额度与官方额度重置窗口（共享 `QuotaWindowsTable`）；卡片与主从列表不并入表格。

### 路由栏

`Dual Relay` 是唯一有意强调表现力的控件。它只出现在概览页顶部，路由条顶部为「当前启用配置」标题（2026-08-31 用户指令确立标题形态并复用面板标题令牌，2026-09-02 用户指令由「当前路由」改名，文案与区域 `aria-label` 同源），下方由两张实心客户端路由卡组成，嵌在实体轨道路由条内。即使仅选中其中一个页面，也应同时展示两个客户端，因为二者关系是产品的核心事实。

### 用量工作台

- 独立「用量」页只读取本机 Codex / Claude Code 会话记录，并按「汇总指标 → 模型构成 → 本地 token 日趋势 → 精确明细」排列；图表不与供应商余额或官方额度并置、更不互相换算。四项汇总指标、模型构成、日趋势和精确明细都是有明确间距的独立模块，不共享边缘或连接式外框；模型构成以左侧环形总量与右侧模型、占比、token 值账本对应，日趋势为独立通栏分析面。没有可信时间戳的 token 仍计入汇总和明细，但明确不进入日趋势。
- 用量页按时间范围从后端持久、无凭据的本地会话快照读取最近成功报告；再次进入同一范围先展示标明时间的快照。后端是快照有效期的唯一所有者，返回每项 `refreshAfter`；页面仅在可见时据此安排后台重扫。手动「刷新」始终绕过该范围的快照；失败时保留已显示结果并说明读取失败。快照写入独立 `model-usage-cache.json`，损坏时重新汇总并原子替换；它不属于供应商余额、官方额度或 token 历史账本。
- 本地模型 token 的数值格式由 `src/lib/token-format.ts` 唯一拥有（2026-09-04）：汇总卡、趋势卡和模型构成使用明确带 `tokens` 单位的 K / M / B 紧凑值（如 `225.7M tokens`）；明细表的 token 列标题标注 `tokens`，单元格保留完整千分位数，供核对真实计数。不得在组件内各自缩写或省略单位。
- 供应商用量与官方额度的通用数值 / 单位格式由 `src/lib/usage-format.ts` 唯一拥有（2026-09-05）：表格、趋势标题与比例轨对同一读数保留至多两位小数并附其来源单位；`UsageTrendChart` 的调用方必须以 `valueKind` 显式声明 `generic`、`local-token` 或 `percentage`，本地模型 token 仍遵循上条专属格式，不能仅因字符串单位相同而混用展示语义。
- 图表层接入 BoardUI（https://boardui.com，MIT 源码分发；2026-09-04 用户指令取代先前的「仅自建图表」约束）：`styles/boardui.css` 是唯一接线入口——按 Vite 方式加载 Tailwind v4（`@tailwindcss/vite`），**不引入 preflight**，以 `.bui-scope` 承载 BoardUI 组件所需的复位；暗色从应用 `data-theme` 桥接而非 BoardUI 的 `.dark`；`--color-chart-*` 序列色重映射到 `Frosted Relay` 序列色（行动蓝 / Claude 紫 / 安全绿 / 警告橙），图表令牌不属于第二个视觉语言。KPI 行为 BoardUI `StatCards`（本仓库改型：没有真实对比数据时不渲染涨幅徽章）。`styles/theme.css` 是隔离的 BoardUI 供应商基础层（令牌所有权测试豁免的唯一例外）。
- 全局 `UsageTrendChart`、`ModelUsageDistributionChart` 与 `UsageRatioMeter` 是唯一图表组件（趋势与构成以 Recharts 按 BoardUI 卡片语言绘制，`UsageRatioMeter` 仍为表格内比例轨）：趋势按同一单位分组并保留可读图例，时间戳取全部真实读数的并集、缺口不插值，单一真实读数渲染为可见数据点；趋势卡大标题只聚合同一时间戳上的序列，只有 `local-token` 语义的同日桶会在每条序列都有该时间点时汇总，独立套餐或额度窗口即使同单位也逐系列展示；静止态显示最新真实读数，绝不跨时间求和；官方额度百分比使用明确的 0–100 纵轴；构成只汇总前五项，其余归为「其他」，图例保留精确数值与百分比；比例轨只显示可计算的真实比例。空数据、单点、长标签、单位冲突与减少动态效果都必须保持稳定布局。
- `state/usage-history.json` 是供应商余额与 Codex 官方额度历史的唯一所有者：仅在真实读取成功后记录数值，保留最近 365 天且每条序列至多 720 点，读取时同样过滤超过保留期的快照。账本不得保存 API key、服务地址、查询脚本、原始响应或账号标识；供应商历史按当前查询摘要隔离，档案变更或删除时清理；官方额度仅在前后账号标记都存在且相同的情况下追加，标记变化或不可比较时清理旧趋势后记录。图表只读账本，不发起网络请求。

### 提供商行

行结构为 CC Switch 式供应商卡（2026-08-27 用户指令 1:1 复刻；配色仍用本系统令牌）：

- 首字母头像方块（44px，中性底）、提供商名称，下方明细分两行（2026-08-28 用户指令）：首行为服务地址链接——显示主机名、行动蓝、悬停下划线，点击经 `opener` 插件在系统浏览器打开（wry 会拦截网页内新窗口，纯锚点无效；权限作用域仅允许 `http/https`），无地址时该行显示路由模式文案；次行为模型，仅有值时显示；
- 行首为拖拽把手（2026-08-28 用户指令，参考 CC Switch 的把手拖动排序；视觉为本系统令牌）：把手是行内唯一拖拽目标，指针移动超过 8px 才启动拖拽，行主体仍是点选；键盘以 Space 抬起、方向键移动、Space 落下完成排序。列表顺序由每个客户端目录中供应商文件的顺序字段唯一拥有；重排必须提交该客户端全部档案及各自版本，不引入聚合档案数组。拖拽中的行加强为行动蓝描边与投影，不缩放。
- 当前供应商身份由后端 `activeProfileId` 唯一判定，Codex 与 Claude 一致：路由、端点及自定义凭据匹配，模型和通用设置不参与；官方路由标记不代表登录令牌有效性。相同连接档案优先按最近切换记录消歧，否则不任意选择。完整配置的 `matchStatus` 独立展示，不用于推断供应商身份。当前卡片＝蓝调实心表面＋安静蓝调软投影，动作簇左侧同时显示「使用中」状态胶囊（文字承载状态，绝不只靠颜色）；卡片边缘保持中性细边，不使用彩色装饰描边或侧边条；
- 变更预览在显式请求前不占位；预览按钮是眼睛开关（2026-08-28 用户指令）——展开后图标变闭眼并带安静蓝色调激活底，再点收回，图标与 `aria-expanded` 共同承载状态，绝不只靠颜色。展开的预览内容渲染在**该行卡片内部、行线下方**（2026-08-28 用户指令，同日由面板底部移入行内）：卡片为纵向两段（行线＋展开区），展开区以细分隔线与行线分界、复用同一卡片表面；「变更预览」标题与「确认切换」主按钮位于展开区头部，其他行不受影响；
- 行尾为图标动作簇（编辑 / 预览或收起预览 / 连通性测试 / 用量 / 删除五个内联线性图标，装入胶囊形容器）：点击直接作用于该行、不改变点选流程；连通性测试点击即检测该卡片的服务地址，检测中禁用本按钮，结果在该卡片内展开；结果显示时再次点击同一按钮收回，收回后下一次点击重新检测；点选卡片更新内部选择并携带选中视觉（2026-09-03 用户指令反转 2026-09-01 的「点选不改变外观」）：选中卡加 `is-selected` 安静蓝调面，行线内挂 `StarlightLayer`（`warm` 琥珀星光 + 暖金辉光，与概览路由卡的冷白配色明确区分，`aria-hidden`，层置内容之下、随 `aria-selected` 浮现），生效行的安静底与投影不变，两种状态可叠加；
- 「启用」主按钮（Play 图标 + 文字，2026-08-28 用户指令，参考 CC Switch 的主按钮形式；令牌为本系统自有）在所有非生效行渲染但默认不展示，仅在行被鼠标悬浮时于动作簇左侧短暂出现（2026-09-01 用户指令）。`aria-selected` 继续只表达选择语义，不参与该按钮显隐；键盘操作经始终可见的预览图标进入相同的预览流程。点击启用拉起变更预览，写入仍需「确认切换」确认弹窗，启用按钮绝不直接写配置。生效行不渲染启用按钮，状态由「使用中」胶囊表达。变更预览面板头部提供「确认切换」主按钮，复用与概览页路由控件相同的确认弹窗。
- 官方登录行在「启用」按钮同位（动作簇左侧、悬浮显现）渲染「重新登录」次级文字按钮（2026-09-02）：展开该行内 `OfficialLoginPanel`（与编辑视图同一组件、同一状态机），`aria-expanded` 承载展开态；完成登录后 `Codex` 行的订阅额度面板换键重查。官方行与自定义行共用同一动作簇（2026-09-03 用户指令）：编辑图标同样渲染并进入编辑视图（名称 / 官网 / 备注与官方登录面板，保存不再要求重新登录——登录凭据由登录流程写入客户端本地缓存，档案写入不触及它；仅新建官方档案仍以登录完成为保存前提）；`Codex` 行另以用量图标收放订阅额度面板（默认展开，收起态经与其他用量面板同一的 `collapsedUsageIds` 持久化，`aria-expanded` / `aria-controls` 指向该面板），`Claude` 官方行无额度数据源，不渲染用量图标；官方行没有服务地址，连通性测试按钮不渲染。
- 卡片常带细分隔边框，悬停转安静蓝色调实心表面；预览展开的行除外——展开卡是阅读表面，悬浮不染色（2026-09-01 用户指令）；点击选中不改变卡片外观；
- 客户端切换是分段胶囊控件（2026-08-28 用户指令移至页面标题行；2026-09-02 用户指令改为标题行下方一栏：左侧分段胶囊、右侧新建主按钮同行对齐；此前一度纵向独立成行，更早紧邻新建按钮左侧）：共享一个容器底座，激活段为白色凸起卡片并带行动蓝描边，未激活段平贴透底；段内容为 OpenAI / Claude 品牌标（官方渠道获取：OpenAI 官方矢量、Claude App Store 官方应用图，商标归属各自公司，仅用于标识集成对象）；
- 新建供应商是客户端切换行右侧的主按钮（2026-09-02 用户指令改为图标＋文字、与 tab 行对齐等高；2026-09-03 用户指令配色改复刻 `spiralcoder` 高级操作按钮）：全圆角胶囊，`+` 线性图标与「新建供应商」文字并排；高度沿用 tab 触达式 `--asb-touch-target - 8px`，表面为碳黑主体＋右侧收敛紫能量区渐变（`--asb-gradient-premium-button`，随主题固定），`--asb-text-on-action` 白字，顶部白色内嵌高光 `--asb-gradient-premium-button-glow(-hover)`，悬停高光增强并整体微提亮（`--asb-premium-button-hover-brightness`），无投影，不再是行动蓝也不再复用 `Tooltip` 暗色三件套；文字在场后悬浮提示与冗余 `aria-label` 随之删除，无障碍名称由可见文字承载；
- 变更预览只在显式请求后渲染——选中或切换筛选不得自动拉取差异；候选预览换选即失效，保证安全切换永远对应可见差异。
- 不得添加装饰性眉题、标语，或“安全 / 本地 / 快速”一类的标签云；
- 不得使用装饰性图标方块或彩色渐变卡片；

### 编辑视图

新建或编辑供应商时切换到独立界面：页面内容替换为「返回按钮 + 标题 + 表单卡」，表单卡铺满工作区宽度，路由条与列表、变更预览全部隐藏，保存 / 取消仍在表单尾部。返回按钮是页面级唯一的图标按钮，文字标题说明当前是新建还是编辑。供应商档案通过显式路由模式区分：新建提供「接入方式」分段选择（2026-09-02 用户需求：自定义 API 中继 / 官方登录；仅新建时渲染，编辑既有档案不提供），默认自定义中继。客户端已拥有官方档案时，点击「官方登录」直接打开该档案的编辑视图以重新登录；不会新建第二个官方档案（每客户端至多一个官方入口仍由后端强制）。选择尚不存在的官方登录后隐藏服务地址、主模型、API 密钥与模型映射字段，表单内嵌入 `OfficialLoginPanel` 发起官方 `OAuth` 登录——`Codex` 走 `OpenAI` 设备码（面板显示验证码并提供「打开验证页面」），`Claude` 自动打开授权页等待本机回环回调；面板以 3 秒轮询直至登录完成，完成前保存按钮禁用；切换到官方模式即清空自定义路由字段，切回自定义从空表单开始，切换客户端则重建为新客户端的空草稿，提交始终走同一条归一化路径。发现或 CC Switch 只读导入同样生成官方登录入口。官方入口不保存服务地址、密钥、模型覆盖或**自定义**用量查询；启用时移除 `openai_base_url` 并将 Codex 缓存恢复为 `chatgpt` 模式。自定义 Codex 切换由执行器在同一确认事务中写入 `auth.json` 的 API-key 字段，保留 OAuth tokens；官方 OAuth 令牌绝不进入档案、界面或日志。

### 表单与配置预览

- 标签应命名用户能控制的对象：“基础 URL”“模型”“API 密钥”“通用设置”。
- 下拉选择的唯一所有者是 `Select` 组件（2026-08-28 用户指令复刻 spiralcoder 的 `Radix Select` 结构，视觉为本系统令牌）：字段形触发器与 `asb-input` 同语言（细分隔边框、控制圆角、悬停转行动蓝），右侧弱化 chevron，未选中时显示占位文案；浮层为实体菜单面、模态投影与顶缘高光，打开即时呈现；选项行 `40px` 触控目标、键盘高亮为安静蓝调表面、当前项右置勾选指示，方向键与 `Escape` 可达。一切下拉选择消费该组件，不再使用原生 `<select>`。
- 避免使用重复可见字段标签或只是在叙述表单的卡片级副标题。
- 路径、`TOML`、`JSON` 和差异使用代码字体与接近实心的背景。
- 配置文件预览逐行换行显示：行号列固定，长行（如超长服务地址）在文本列内折行，任何情况下不得出现横向滚动（2026-08-28 用户指令）。
- 预览渲染各有一个唯一所有者（2026-08-28 用户指令统一化、同日拆分）：`CodePreview`（文件视图：行号 + 令牌着色 + 换行）与 `DiffView`（差异视图：键 + 旧值 → 新值）。一切客户端文件预览（供应商变更预览、备份差异）与通用设置片段预览都消费这两个组件，不得另行重建标记或着色逻辑；通用设置片段不是客户端文件候选。
- 差异视图为红绿语义（2026-08-28 用户指令）：旧值红色文字配浅红 tint 芯片（`--asb-danger` + `--asb-diff-remove-bg`），新值绿色文字配浅绿 tint 芯片（`--asb-diff-add-text` + `--asb-diff-add-bg`，明暗两套令牌），「移除」红色加粗；箭头与「移除」文字保证状态不只靠颜色可读（§10）。旧值缺失（`（无）`）保持中性弱化——无旧值即无移除，不得染红。芯片为近实心底上的着色小块，绝不引入背景模糊。
- 优质打印候选全文归一切预览表面（2026-08-28 用户指令接入供应商变更预览）：`FilePreview.content` 是唯一 UI 来源——执行器只把**按 TOML / JSON 键路径脱敏后的展示副本**交给界面；原始候选文本仍只在执行器内用于 `rendered_hash`、语法校验与原子写入。因此确认仍绑定原始候选哈希，界面绝不显示 API key 或任何宿主既有密钥。供应商变更预览不再平铺「保持不变的设置」键列表：被保留的宿主键在候选全文中原位可见；面板顶部路径标题由文件视图自带的文件条承担，避免重复。`SwitchPreview.preserved` 旧字段已随迁移删除——任何预览表面不得重建宿主键平铺列表，一切配置文本一律经 `CodePreview` 渲染。
- 除供应商编辑器内用户主动点击“查看密钥”的受控输入外，密钥必须渲染为稳定的脱敏令牌，绝不显示部分可见的值。查看状态只在当前表单内生效，切换档案时恢复隐藏；预览、差异、日志、错误和诊断始终脱敏。
- 错误应说明确切字段和恢复操作；不应道歉。
- 时间展示的唯一所有者是 `Time` 组件（2026-08-28 用户指令统一化）：格式 `YYYY年MM月DD日 HH：MM` 本地时区（2026-09-02 用户指令改为中国常用格式，无秒、全角冒号），**不带时区偏移后缀**（时钟读数本就是本地时间，`（UTC+8）` 标注已移除）；`timeLabel` 只是格式化纯函数，扫描测试锁定组件外无任何直接消费者（概览匹配态与上次切换、备份历史、端点验证、撤销/恢复确认全部经组件接入）。
- 端点连通检测有两个入口：供应商编辑器「主模型」右侧按钮栏与供应商卡片右侧操作按钮区（2026-08-31 移植 `CC Switch` 可达性模式）；两处消费同一探测契约，编辑器结果显示在模型操作下方，卡片结果在行内展开。语义为可达性探测——收到任意 `HTTP` 响应即算可达，不发送模型请求、不携带密钥、不验证鉴权或模型配置；仅网络级失败（`DNS` / 拒连 / `TLS` / 超时）判为无法连通，并给出失败类别。结果三态：连通正常＝安全绿、连通但较慢（延迟超过 6 秒）＝警示黄、无法连通＝警示红；「结论 · HTTP 状态 · 延迟 · 时间」文字同时在场，结论绝不只靠颜色可读（§10）。超时类失败自动重试一次后定论；每次检测后附一行说明文字，声明可达不代表密钥或模型配置正确。编辑器内该按钮与「获取模型」保持固有宽度，绝不挤占主模型输入框的弹性宽度（2026-09-03 用户指令：模型行改为弹性换行布局，检测按钮过宽导致其他按钮偏移的旧栅格布局已删除）。
- 「当前用户级配置模型」是供应商列表、编辑与预览的固定上下文：命中真实配置的供应商卡显示此模型而非档案保存值；编辑器在主模型输入框下方以弱化说明行显示它（客户端使用默认模型时不显示该行），变更预览顶部以键值行显示同一事实（空值显示「默认模型」）。三处均消费应用快照中该客户端 `ConfigFileStatus.route.model`，绝不重新推导或读取草稿值。它只描述已读取的用户级配置文件，绝不声称是运行中会话的模型；`ConfigFileStatus.route.scopeWarnings` 非空时，编辑器与预览必须显示范围警告。
- 快捷模型选择的唯一所有者是 `ModelPicker` 组件（2026-09-03 用户指令移植 `CC Switch` 的模型下拉）：获取模型后，主模型与 `Claude` 三档映射（Haiku / Sonnet / Opus）的输入框旁各出现一个字段形 chevron 图标按钮，点开为可搜索浮层菜单——顶部搜索框按模型 `id` 与厂商名过滤，模型按 `owned_by` 厂商分组（后端 `/v1/models` 契约唯一所有者 `probe::ProviderModel`），无厂商归属的归「其他」并固定在末尾（不随用户环境排序变化）；组名作小号弱化标题，当前值右置勾选，交互契约（方向键、`Escape`、外点关闭、回焦）与字体选择器一致，浮层与搜索框共享同一菜单材质。菜单只负责选中，清除模型一律直接编辑输入框；旧的「（从获取列表选择）」`Radix Select` 选择路径已随本轮删除。
- `1M` 启用勾选内联在其模型行的右侧（2026-09-03 用户指令），不换行独占一行：可见文案固定为紧凑的「`1M`」，完整语义（如「主模型启用 1M 上下文」）经复选框原生的 `aria-label` 保留为无障碍名（`Checkbox` 组件的唯一扩展点）；主模型的 `1M` 位于快捷下拉与端点操作之间，三档映射的 `1M` 位于各自输入框行内，`Codex` 的上下文窗口勾选同位显示，独立的「模型运行参数」字段集随内联化删除。
- 用量查询位于供应商卡片右侧的操作按钮区，不在供应商编辑器中出现。它是无文字的单图标按钮，文字只在悬浮提示与无障碍名称中保留；未配置的卡片点击后直接进入独立的完整工作区且不占额外空间，已配置的卡片默认显示并读取账本读数，图标用于收起或再次展开。收起选择按档案持久化（2026-09-02 用户指令）：收起档案的集合由应用设置的 `collapsedUsageIds` 唯一拥有并经同一保存路径落盘，重启后保持；未收录的档案一律默认展开。配置为选填，并在「字段提取」和「自编脚本」两种模式间二选一：字段提取沿用一次 `GET` 请求、`{{baseUrl}}` / `{{apiKey}}` 占位符与余额 / 已用 / 总量路径；脚本是受限 JavaScript，只能生成一次 `GET` / `POST` 请求并从 JSON 响应提取一组或多组具名数值，网络请求仍由应用执行。独立工作区自身拥有草稿和「保存查询」动作；保存后回到供应商列表，不与编辑供应商草稿耦合。已保存供应商的刷新由后端仅凭 `profileId` 解析档案、执行查询；成功结果同时更新当前进程的托盘摘要缓存和应用自有历史账本，渲染器不再传递档案密钥。历史曲线按当前查询摘要读取，配置被编辑、删除或重置后旧曲线随之清理。读数在卡片内以「用量 / 更新时间」标题行、真实余额趋势和全局用量读数表组成（2026-09-02 用户指令，唯一所有者 `UsageReadingsTable`；同日用户指令移除标题行的查询类型徽标——字段提取 / 脚本的区别只在配置工作区呈现）：列为「额度 / 余额 / 已用 / 总量 / 占比」，空值统一显示「—」，每组套餐一行；只有可计算比例时占比列才显示细进度轨，标题行右侧保留「编辑查询」与「刷新」；查询工作台的「本次结果」以同一张读数表呈现。密钥只随受管请求传输，绝不出现在结果、错误或诊断里；配置仅存于应用档案存储，绝不写入任何客户端配置文件。每个已配置的查询自带「自动刷新间隔」（`refreshIntervalMinutes`，2026-09-03 用户指令，随档案保存而非应用设置：字段提取与自编脚本两种模式共用，工作台内以数字输入编辑，任意 0–1440 整数分钟、`0` 为仅手动，越界输入回退不落盘，校验唯一所有者 `asb-core validate_usage_query`；字段为严格必填，供应商文件在字段加入前的上一版形态由存储迁移 `migrate_usage_query_interval` 一次性原子补入 `0` 并写回（`interfaceFont` 同款模式，非运行时缺省读），其余任何形态照旧明确拒绝；CC Switch 导入的查询同样从手动开始，其自身的 autoQueryInterval 归入未导入键警告）。已配置的供应商卡片在列表挂载期间按该间隔查询，无论详情展开还是收起；收放不额外查询或重置计时，间隔为 0 时仅首次读取及手动刷新。卡片通过 `useProviderUsage` 持有唯一查询与历史状态，详情和收起摘要共用结果，读取状态机仍由 `useAutoQuery` 负责。收起时模型信息栏显示各套餐的剩余百分比及余额；无有效总量时只显示真实余额或已用量，不推算百分比、不合并不同套餐或单位；更新失败保留上次读数并注明失败，查询配置变化后清除旧读数并重新查询。离开供应商列表、切换客户端或进入编辑界面时对应卡片卸载，查询停止。
- Codex 官方登录卡另有原生「订阅额度」表，不复用、也不能配置为上述第三方查询。仅该卡在后端临时读取 Codex 既有 OAuth 身份，向服务端查询限额窗口；返回值严格收敛为「窗口名称、已用百分比、重置时间、查询状态」，令牌和账号标识不进入渲染器、档案、缓存、日志、预览或错误。卡片首次显示时读取一次，之后只提供手动刷新、没有轮询（官方读数无查询配置界面，也就没有间隔设置）；窗口经唯一所有者 `QuotaWindowsTable` 以「窗口 / 已用 / 重置 / 占比」表格行显示，趋势图只展示成功读取后写入的同一窗口百分比序列。未登录、身份过期与服务端拒绝分别给出可恢复状态（重新登录 Codex 或稍后刷新），不清空上次成功读数；官方账号标记变化时先清空旧趋势，再记录新账号的读数。界面不声称该非公开服务端接口是稳定的官方 API。

### 通用设置（2026-09-01 契约重塑）

- 页面先以独立主 `Tab` 在「基础参数」「官方设置目录」与「全局指令」之间切换（2026-09-02 用户指令：全局指令为第三个主 `Tab`，不再叠放在官方设置目录下方）；三者不共享保存动作或草稿。页面顶部保留 `Codex` / `Claude` 双 `Tab`（复用供应商页的品牌图标分段样式），三个主 `Tab` 的内容均跟随该客户端选择。
- 官方设置目录从同一后端所有权目录展示每个官方配置族的真实边界：`基础参数` 是已有类型化用户偏好，`独立模块` 是拥有专属事务的资源，`保留不写入` 则是项目、本地、受管、登录或尚无专用编辑器的结构。目录不是原始 `config.toml` / `settings.json` 编辑器，不提供第二条写入路径。
- 「全局指令」（2026-08-31 用户指令）直接管理两个客户端真实的全局工作约定：`Codex` 的 `$CODEX_HOME/AGENTS.md`（未设时为 `~/.codex/AGENTS.md`）与 `Claude` 的 `~/.claude/CLAUDE.md`。不建立模板库、启用开关或第二份持久化副本；文档本身是唯一真相。
- 「全局指令」主 `Tab` 内不设第二套客户端切换，文档跟随页面顶部客户端 `Tab`；面板仅显示文件名，不向渲染器暴露绝对路径。编辑器采用高对比度近实心代码面，按内容从 `320px` 自动增长至 `min(58dvh, 640px)`；超过上限后只在编辑器内滚动，绝不无限拉长页面，窄屏相应收为 `240px` 至 `min(56dvh, 512px)`。`重新读取`、`放弃草稿`、`保存 AGENTS.md / CLAUDE.md` 是独立操作，未保存草稿时禁止重新读取，避免无提示覆盖。
- 文档读取带内容哈希；保存由后端解析目标路径后执行锁定、外部改动比对、隔离备份、临时写入、原子替换与写后逐字校验，校验失败恢复紧邻备份。提示词备份不混入客户端配置备份列表。
- 每项通用设置记录应用拥有的明确意图：`自动` 代表由客户端与当前模型决定，`显式值` 才会随供应商事务写入对应字段；它不读取或展示真实配置文件行。页面最底部的预览模块只有一个控件（2026-09-02 用户指令「保留一个即可」）：「查看通用配置预览」首次点击按当前草稿以客户端实际格式渲染仅含显式通用值的只读片段（`aria-expanded` / `aria-controls`），显示中变「收起通用配置预览」，收起后变「展开通用配置预览」并复用缓存片段、不再重复渲染请求；草稿一变片段即失效，按钮回到「查看通用配置预览」。片段不读写宿主文件、不含供应商或宿主字段，因而不等同于真实客户端文件候选。
- 基础参数只承载能由一个受限控件表达、并且可随供应商事务安全投影的官方用户偏好项：布尔项以「自动 / 开启 / 关闭」三态分段表达，多值项以「自动 + 合法档位」表达；推理强度保留专用滑块，首档为自动并在右上显示「当前推理：{档位}」，其余档位使用分段药丸（共享底座 + 白色凸起激活段 + 行动蓝描边，与客户端 `Tab` 同语言；`role="radiogroup"`，键盘可达）。Claude 的 `Ultracode` 是独立布尔设置，不属于 `effortLevel` 的滑块档位。目录其余结构化族必须先取得专属编辑器、验证和事务契约，不能伪装为普通开关。
- 保存仅更新固定通用设置文件：加载失败可重试、保存失败保留草稿；保存成功显示已保存的版本、已应用或待重新应用，并在真实配置被外部修改时明确标识。参数表单的页尾操作由页面唯一拥有：左侧「全部恢复默认值」，右侧依次为状态与「保存通用设置」；分组只提供本组的「恢复默认值」。恢复默认值的实际语义是将该组回到自动行为。待重新应用或真实配置异常直接替换通常保存状态，不出现第二行或跨页面跳转按钮。通用配置预览保留在工具栏之后的独立分隔区。查看片段不产生备份或配置写入记录；供应商页的候选预览与「启用/重新应用」才将固定通用设置和完整供应商声明合成为 `~/.codex/config.toml` 或 `~/.claude/settings.json`，并执行完整事务。
- 推理强度滑块使用本产品的胶囊轨道、能量渐变填充（`--asb-gradient-energy`，安全绿→行动蓝→Claude 紫）、填充内九颗微光粒子、刻度与白色滑钮；唯一所有者是 `Slider` 组件（2026-09-03 用户指令补齐 `spiralcoder` 契约）：入口校验有限数值 / max>min / step>0 / 值在域内（违反即抛错），量程超过 13 档时刻度压缩为 7 个采样点；按住原生输入时动效时长切至 `--asb-motion-press` 且滑钮 `0.96` 按压，松手回到常规时长；动效随 `prefers-reduced-motion` 关闭。
- 选项目录唯一所有者是 `asb-core::ownership::official_setting_directory`；其中可直接编辑的字段由同模块的 `setting_specs` 同时拥有所有权、控件、值约束、分组与供应商缺席清理。界面不得自建键列表。模型行为参数（推理强度 / 推理摘要 / 回复详细度）全部归基础参数，档案编辑器的运行参数只保留上下文窗口；模型路由仍归供应商档案。
- 供应商路由与凭据（归供应商页与客户端自身）、`MCP` / 权限 / 沙箱策略表、路径与自由文本键、项目/本地/受管来源和宿主内部状态都必须在官方目录中明确标示真实边界；未有专属模块前保留而不写入。

### 设置（2026-08-28 用户指令）

- 顶层「设置」页只管理 Agent Switchboard 自身，不混入 Codex / Claude Code 的通用设置；当前组为「外观」、「窗口与托盘」、「性能」与「软件更新」。
- 「界面主题」为 `跟随系统 / 浅色 / 深色` 分段单选：显式浅色压过系统深色、显式深色压过系统浅色，跟随系统维持媒体查询；「动态效果」为 `跟随系统 / 减少动态效果`，后者停止现有路由、滑块和控件强调动画。
- 「界面字体」（2026-08-31 用户指令，交互参考 `ZiChe` 的字体选择器、视觉为本系统令牌）为 `FontPicker` 下拉：字段形触发器以当前字体渲染字体名；菜单为实体菜单面，顶部搜索框过滤，选项 = 随应用打包的 `Noto Sans SC` + `list_system_fonts` 枚举的已安装字族（去重），每个选项以该字体本身渲染「字体名 + 中文 Aa 012」示例——浏览列表即是预览；每行预留固定宽度的右置勾选槽位（当前项显示勾选，样本列不随勾选位移），方向键 / `Home` / `End` 在列表内移动焦点，`Escape` 与外部点击关闭。选择即经完整设置对象保存并立即应用于展示与界面文字；代码保持等宽字体。字族枚举唯一所有者是 `src-tauri/src/fonts.rs`（`GDI` `EnumFontFamiliesExW`，跳过 `@` 竖排字族，排序去重），枚举失败时仅保留打包默认项，不阻塞设置。
- 「点击关闭按钮时」为分段单选：`最小化到托盘`（默认，关闭后继续运行并由托盘恢复）或 `退出应用`（关闭即结束进程）；「窗口始终置顶」与「开机自动启动」为即时开关（与「启用硬件加速」同款）。置顶先调用跨平台 native 窗口接口；开机自动启动先通过 Tauri autostart 插件实际注册或取消系统登录启动项；任一步失败都会恢复先前的桌面状态且不落盘。顶栏置顶快捷钮与该开关是同一设置的两个视图：图钉钮位于导航项与窗口三钮之间，未置顶灰色图钉 / 置顶行动蓝并携带 `aria-pressed`，提示与可及名「置顶窗口 / 取消置顶」；点击经同一 `set_app_settings` 完整对象路径持久化（先 native 后落盘），设置未加载前不渲染，保存失败保持原视觉状态。
- 设置页布尔行为即时开关（2026-08-31 用户指令复刻，同日按操作逻辑定稿为文字在左、开关在右）：行结构与分段 / 字体行同构——标签与说明在左列，`Switch` 控件在行右缘。`Switch` 组件（`role="switch"`，可及名经 `aria-label`，隐藏原生 input 保留键盘与读屏行为）只绘制控件本体：44×26 药丸轨道 + 18px 白滑钮（`--asb-slider-thumb-bg`）滑动 220ms；开启轨道换装移植的 spiralcoder 主按钮渐变（青→蓝→粉 88° 五停 `--asb-gradient-button`）加内嵌蓝光晕，悬停白光增强（`--asb-gradient-button-glow(-hover)`），与供应商行「启用」主按钮同一渐变身份；关闭态为画布底 + 发丝边的中性轨道，悬停边框转行动蓝。按下轨道 `0.98` 缩放；圆环仅键盘导航时显示；减少动态效果时滑动与换装瞬时完成。`Checkbox` 继续拥有通用设置与发现页的复选框语义，二者不混用。
- 「启用硬件加速」控制 WebView2（Windows）的 GPU 渲染；该设置分组仅在 Windows 渲染（`navigator.userAgent` 判定），其他平台没有对应的 WebView 创建参数可暴露，`AppSettings` 字段仍保留在契约内。因浏览器参数仅在 WebView 创建时读取，用户在当前设置页打开或关闭该开关后，性能分组才紧邻显示「重启以应用硬件加速 / 重启应用」操作行，由用户自主决定是否立即完整重启。点击调用后端唯一 `restart_application` 命令重启整个桌面进程，不以页面刷新或最小化到托盘替代。关闭时在 Wry 默认浏览器参数上追加 `--disable-gpu`；若窗口已有显式参数则保留它们并追加该开关。
- 主窗口在原生创建阶段以 `center: true` 居中，避免首次打开落在屏幕下方或先显示后跳动；窗口运行中由用户拖拽后的坐标不被页面代码干预。
- 「软件更新」在应用挂载后静默检查一次 GitHub `latest.json`，不显示全局忙碌遮罩，也不因网络失败打断启动。Tauri Updater 以应用内置 Minisign 公钥校验清单与载荷签名；发现新版本时，顶栏导航与置顶钮之间出现行动蓝「更新」按钮与绿色状态点（提示与可及名「发现新版本 {version}」），点击跳转设置页。设置分组始终保留「检查更新」次级按钮；检查中显示「正在检查新版本」，检查后原位展示「已是最新版本」或「发现新版本 {version}」并附当前版本与检查时间（本地时区）。发现新版本时同一行追加「下载并安装」主按钮，展示已知总大小的百分比或未知总大小的已下载 KB；下载、验签与安装只由用户点击触发。Windows NSIS 成功后由更新器退出、安装并自动重启；macOS / Linux 安装完成后调用同一应用重启命令，若该命令失败则显示「更新已安装，需要重启」与「重新启动」按钮，绝不再次安装同一载荷。每次检查替换、安装结束或组件卸载均释放对应更新资源；仅用户主动检查、下载、安装或重启失败走全局错误通知。
- 设置值以完整严格对象原子保存到应用数据 `state/settings.json`：`closeBehavior`、`theme`、`motion`、`alwaysOnTop`、`launchAtLogin`、`hardwareAcceleration`、`interfaceFont`、`runtimeLogLevel` 全部必填；`collapsedUsageIds`（2026-09-02 用户指令，供应商用量面板的收起档案集合）是唯一例外字段——旧文件缺省该字段时确定性地读作空集（全部展开），其余偏好不受影响，一经写入即与必填字段同等严格；`interfaceFont` 必须是非空、无首尾空格、不含引号 / 反斜杠 / 控制字符且不超过 64 字符的字体名；除此以外的任何旧形态、不完整形态、未知字段和非法枚举值都明确拒绝，不存在旧设置迁移、双读或默认字段补写。绝不写入客户端配置文件、供应商档案或通用补丁。
- 设置组使用紧凑实心信息行，与通用设置的分段语言一致；不会显示虚构的配置代码行或候选文件预览。设置行不携带说明性副文（2026-08-31 用户指令移除全部描述行）——标签与控件即一行设置的全部内容；唯一例外是检查后的版本 / 时间结果与失败态的恢复指引，二者是状态而非描述。分组之间的 `24px` 结构间距唯一所有者是 `.asb-app-settings`（设置页只有这一个容器根，表单、失败行与软件更新组都是它的直接子项；§5 结构间距令牌供值）。
- 设置读取失败（2026-08-31 用户指令；2026-09-03 全局横幅）时设置页固定显示失败态而非停留「加载中」：同款紧凑实心信息行携带 `role="alert"`——标签行「设置加载失败：{原因}」、说明行「读取失败期间，外观与关闭行为使用默认值」与「重试」次级按钮（重新拉取，成功后原位替换为表单）；加载成功前顶栏置顶钮维持不渲染。全局错误通知照常发出，页面内文字是持久来源。同一失败同时以工作区顶部的持久错误横幅出现在**每一页**（2026-09-03 用户指令）：横幅为「应用设置不可用：{原因}」加次级「一键修复」按钮，直接调用同一 `repair_app_settings`——设置类操作（用量收起、置顶、日志阈值）在设置不可读期间静默等待，横幅让阻塞原因与恢复入口在任何发生处可达；横幅与设置页失败行是同一修复动作的两个视图（与顶栏置顶钮 / 设置页置顶复选框同款双视图关系），修复成功或重试成功后横幅随加载状态消失。

### 会话管理（2026-08-28 用户指令）

- 顶层「会话」页只覆盖 `Codex` 与 `Claude Code`，与产品支持范围一致。列表支持关键词与客户端筛选，默认由最近活跃时间排列。
- 选中一条会话才加载其对话历史；详情显示客户端、会话 ID、最近活跃、已记录工作目录和恢复命令。会话源路径不在界面或前端契约中暴露。消息目录只列用户的真实提问并逐项定位到原文：`Codex` 以用户角色记录的非对话载荷（`AGENTS.md` 指令转储、环境上下文、`IDE` 上下文包装）不进入目录；`IDE` 包装内嵌的请求段（最后一个匹配标题的负载）作为该条预览，提不出请求段的整条隐藏。`Claude Code` 会话不做此清洗。
- 行操作包括「在命令提示符中恢复」、复制恢复命令和（可得时）复制工作目录。「在命令提示符中恢复」以新窗口启动受控的客户端恢复命令，工作目录可用时直接作为进程目录传入；不提供删除会话、修改历史、会话数据库写入或任意命令执行入口。
- 来源缺失或不可读是行内提示，不替代其它客户端的可读结果，也不伪造空数据。
- 会话列表与对话内容为运行期内存缓存，缓存期为 30 秒：期内切换页面或重新选中最近查看的会话直接显示缓存、不发请求；过期后先显示缓存再后台刷新，列表不回到扫描占位。对话内容缓存至多保留最近查看的 16 条，超出时淘汰最旧条目。「刷新会话」始终强制重扫。缓存不落盘，应用重启后首次进入照常扫描（2026-08-31 移植自 `CC Switch` 的查询缓存模式，由前端重新实现）。

### 操作

所有操作按钮的唯一所有者是全局 `Button` 组件（2026-09-03 用户指令复刻 `spiralcoder` 按钮体系，`src/components/Button.tsx`）：组件拥有几何契约，总是输出 `.asb-btn` 基座加恰好一个变体表面类——基座持有 `40px` 触达高、最小宽 `88px`、行动圆角、`6px` 图标间距、居中 `inline-flex` 与默认 `type="button"`（提交按钮显式传 `type="submit"`）；变体（primary 行动蓝 / secondary 中性描边 / danger 红色 / icon 方形触达 / plus 黑紫高级胶囊 / back 方形返回）只在 `base.css` 持有表面与几何差量，业务文件不得再手写 `asb-btn-*` 类名。

| 操作 | 处理方式 |
| --- | --- |
| 确认切换 | 行动蓝填充，白色标签，位于变更预览标题行，必须确认（概览路由栏的切换入口已于 2026-09-02 移除） |
| 取消切换 | 中性描边操作，位于变更预览标题行「确认切换」左侧，点击收回变更预览、不写入任何配置（2026-09-02 用户指令） |
| 查看差异 | 安静实体面 / 实心次级操作 |
| 恢复备份 | 中性描边操作，必须确认 |
| 打开备份文件夹 | 中性描边操作，直接唤起系统资源管理器，无确认 |
| 撤回上一次切换 | 系统红色填充，白色标签，必须确认（2026-08-28 用户指令） |
| 删除提供商 | 系统红色用于入口与确认流程 |

### 备份（2026-08-31 用户指令）

- 备份页使用「本地备份」与「加密云端备份」双 `Tab`；默认显示本地历史。本地页签独占撤回与打开备份文件夹操作，云端页签独占 Supabase 表单；切换页签不会写入或清除本地档案。`Tab` 复用既有分段控件的近实心底座、白色激活面与行动蓝描边，不增加导航或设置页入口。
- 云端页签不是嵌套材质卡片，复用本页的近实心内容面与既有表单控件。
- 云端页签在表单前默认展示「从零配置 Supabase」的有序教程：创建项目、只复制 `Project URL` 与 publishable key、在 `Integrations → Data API` 确认 `Enable Data API` 已开启后执行应用给出的初始化 SQL、在 Dashboard 直接创建项目 Auth 用户、填写并测试、保存后以备份密码上传或恢复。教程指定 `Authentication → Users → Add user → Create new user` 并保持 `Auto Confirm User`，避免无网页回调的桌面应用误走邮件邀请及默认 `localhost` 回跳。它必须明确区分项目 Auth 密码、Dashboard / GitHub 登录密码与备份密码，并明确禁止使用 Account Access Token、secret / service-role key 或数据库密码；教程是紧邻字段的文字序列，不做第二张卡或可折叠的隐藏流程。
- 本地页签填满工作区可用高度：撤回、打开文件夹与上次操作说明固定在模块内顶部，长备份历史仅在自身表格容器滚动；不得以外层页面滚动来查看备份行。
- 撤回确认弹窗在确认前展示“当前配置 → 将恢复备份”的受管键差异（复用 `DiffView`）；差异仍在读取或读取失败时，“确认撤回”禁用，用户只能等待或取消。差异较长时只在弹窗明细区滚动，确认 / 取消始终可见。
- 用户填写自己 Supabase 项目的 HTTPS 地址、publishable key 和项目 Auth 登录邮箱；「显示初始化 SQL」原位展示唯一的表结构与 RLS 策略，用户在自己的 Supabase SQL Editor 执行。应用绝不请求或保存 Supabase secret/service key。
- 「测试连接」使用当前草稿与临时项目 Auth 登录密码验证登录和当前用户对备份表的读取权限；它不要求先保存草稿，不创建或替换云端备份，成功后保留登录密码在当前窗口内存以便随即备份。
- 项目 Auth 登录密码与自行设置的备份密码只在当前操作输入框中暂存，上传或恢复成功后立即清空，且永不持久化。远端只保存经 Argon2id 派生 AES-GCM 密钥加密的完整应用配置目录：两个客户端的全部供应商档案（含端点、模型和 API 密钥）、通用配置与切换记录；不上传 Codex / Claude Code 原始配置文件或本地文件备份。
- 「备份到云端」与「从云端恢复」均先经确认弹窗。恢复是红色确认：替换本机供应商档案、通用配置和切换记录，但不修改当前实际客户端配置；恢复后必须重新预览才可写入客户端。

### 全局通知（2026-08-31 用户指令，复刻 spiralcoder）

结构复刻 spiralcoder 的 toast 系统，视觉为本系统令牌。`use-toast.ts` 是唯一契约所有者（模块级 store，任意层可直接调用 `toast()`，无需 context）；`Toaster` 在应用根挂载一次。

- 栈：视口左上角悬浮（2026-08-31 用户指令，避开顶部居中的导航），最多同屏 2 条，超出淘汰最旧；错误置顶且绝不因新通知被挤出。栈本身 `pointer-events: none`，通知表面恢复指针事件。
- 种类与图标：info / success / warning / error 四种手绘线性图标，色调分别为行动蓝 / 安全绿 / 警告橙 / 系统红；error 用 `role="alert"`，其余 `role="status"`。
- 自动关闭：info 4.5s、success 3.2s、warning 6s、error 10s（2026-08-31 用户指令设定错误关闭时长，比警告更长以留足阅读后端错误信息的时间）；`durationMs: 0` 可对单条强制手动关闭。
- 暂停契约：悬停、聚焦或页面隐藏时暂停计时并保留剩余时长，离开后继续。
- 材质：实体菜单面（`--asb-surface-menu`）、模态投影、顶缘高光；标题半粗、副文转弱（仅有副文时保持实色）；右上关闭钮 40px 触控目标。
- 呈现：通知新增与移除均即时完成，不建立退出状态或动画计时器。
- 层级：`z-index: 70`，位于表单确认页与工具提示之上。
- 与操作横幅的分工（2026-09-01 收敛）：横幅（§7）只承载带决策入口的持续错误；toast 是全部一次性操作反馈的唯一通道——写操作失败（error，含 message）、切换 / 恢复备份 / 撤回的结果（成功为 success「已切换到「X」/ 已恢复备份 / 已撤回上一次切换」，有警告时合并为一条 warning，标题带警告条数、每条警告一行）。通用设置保存成功以页面状态呈现，失败保留草稿并显示错误；发现页 CC Switch 导入结果保留面板内嵌展示（含未导入明细），不发全局通知。

## 9. 动效与状态

- 按下：只改变背景、边框或文字颜色，不缩放控件。
- 路由节点移动：`220ms`、标准缓动，仅在确认后发生。
- 面板进入：即时呈现，不做位移或缩放入场。
- 悬停：背景和细分隔线变化；不得使用卡片抬升动画。
- 焦点：带可见偏移的 `2px` 行动蓝圆环。绘制类选择控件（复选框、开关、分段药丸、滑块）的圆环仅在键盘导航时显示——复选 / 开关 / 单选 / 滑杆输入被鼠标点击时原生匹配 `:focus-visible`，环会包裹每次点击，故由输入模式标记（根元素 `data-focus-source="key"`）区分：键盘导航键按下时标记、任何指针按下时清除；表单文本输入的焦点环不受影响（2026-08-28 用户指令）。
- 减少动态效果：移除路由移动；状态变化保持即时且可见。

## 10. 无障碍与性能

- 最小交互目标为 `40px`；仅图标按钮为 `44px`。
- 所有文字均达到面向 `WCAG` 的对比度，包括紫色身份标签。
- 绝不能只用颜色表示启用状态：还需包含文字、圆点或轨道位置。
- 运行时不得使用 `filter` 或 `backdrop-filter` 读取、模糊或重绘背景像素。
- 低功耗条件和请求减少动态效果时，禁用高成本的环境背景移动。
- 提供键盘导航、可见焦点环，并使用 `Escape` 关闭面板和对话框。

## 11. 应当与禁止

**应当**

- 让配置数据保持视觉焦点。
- 用留白、细分隔线和材质深度建立层级。
- 让简短标题与实际设置 / 值共同说明面板。
- 让下一项不可逆操作清晰无误。
- 在相关页面始终显示启用的路由。

**禁止**

- 使用彩虹渐变、动态粒子、霓虹光晕或通用指标卡片。
- 模糊代码块、差异文字或表单文字；主界面不采样背景像素，托盘只采用本文件明确规定的操作系统材质。
- 用半透明采样材质代替信息层级。
- 将标签堆、眉题、通用副标题或解释性填充文案用作视觉装饰。
- 在组件中增加第二种操作色或新的圆角尺度。
- 将路由轨道变成装饰性动画。

## 12. 设计评审说明

对于“Apple 式层级”，最初直觉往往是全屏磨砂玻璃。这会让配置管理器变得含糊且令人疲劳。本系统有意只把视觉强调用在一个有意义的元素——`Dual Relay`——其余配置与审计界面则以直接绘制的实体面、细边和内高光保持平静、清晰。同样的克制也适用于文字：移除装饰性标签和副标题文案后，信息层级会更清楚，而非更弱。

## 13. 安装体验与三端打包

打包契约按平台拆分：`tauri.windows.conf.json`、`tauri.macos.conf.json`、`tauri.linux.conf.json` 分别拥有内部 NSIS 部署引擎与 WebView2Loader 资源、`["dmg", "app"]`、`["deb", "appimage"]` 三份 bundle 契约。`npm run tauri:build:<平台>` 是唯一构建入口；Windows 通过 `scripts/build-windows-installer.ps1` 编译引擎后嵌入自绘引导，再对最终引导签名，发布阶段只读取 `bundle/installer`，不发布内部引擎。macOS 的 `app` 目标负责生成签名 `.app.tar.gz` 更新载荷，`dmg` 继续作为人工下载安装包。

Windows 安装器是独立的全自绘 WPF 窗口，不依赖应用或 WebView2 已安装。它只完成安装这一件事：说明正在安装什么、选择位置、展示真实安装状态，并在完成后让用户决定是否启动应用。macOS 出 Apple Silicon 与 Intel 两个 dmg；Linux 出 deb（声明 `libwebkit2gtk-4.1-0` 运行依赖）与 AppImage。CI 矩阵构建四个产物组，逐产物扫描；标签发布仅在整个矩阵及所有签名载荷检查通过后执行。

- 引导使用 Agent Switchboard 自有双半宝石标识、圆角自绘控件、清晰留白与单一行动按钮，不显示第三方商标或传统向导侧栏。`installer/Theme.xaml` 唯一拥有安装器颜色、字体与控件模板；不保留旧 NSIS 页头/侧栏位图。
- 安装引导与托盘遵循同一单层外轮廓规则：DWM 拥有窗口圆角并关闭系统描边，WPF 根 Grid 铺满客户区，不绘制第二层圆角 Border；WindowChrome 不增加圆角或缩放边框。内部表单控件的边框不受影响。
- 安装引导顶部 60 逻辑像素为系统标题拖动区域；最小化与关闭按钮单独参与点击命中，安装进行中仍可最小化。窗口不允许缩放。构建必须先构建当前应用部署引擎再嵌入引导，不允许仅凭相同版本号复用旧引擎。
- Windows 11 支持时使用官方 DWM TransientWindow 材质；系统不提供该能力时同一布局采用实心可读底色，不切换另一套界面。标题栏、安装、进度与完成界面全部由 WPF 绘制；系统权限提示和文件夹选择器仍由操作系统提供。
- 引导默认仅为当前用户安装，已有安装位置从原安装登记读取。内部 NSIS 始终静默执行，负责文件部署、升级、快捷方式和卸载登记；WebView2 检查及缺失时的静默下载安装由引导唯一拥有。安装进行时禁止强制中断部署，没有假百分比；使用真实活动状态，减少动态效果时静止显示。
- `/P` 自动进入自绘进度，`/S` 全程无窗口，`/UPDATE` 与 `/NS` 传递给部署引擎。`/R` 和 `/ARGS` 由引导在部署成功后启动应用，避免二次 NSIS 参数解析；手动 `/D=` 必须是未加引号的行尾路径，`/ARGS` 后的内容全部视为应用启动参数。安装成功与启动失败是独立状态，不把启动失败误报为安装失败。
- 完成后启动默认不勾选。简体中文与英语随系统语言选择；错误显示可操作原因，失败可重试。安装器图标与引擎卸载器图标继续由 `src-tauri/icons/icon.ico` 提供。MSIX 不使用此引导，由 Store 自身管理。
